# 审查归档 — UC-E2E-011 报告失败退款/计费边界 · mw-e2e-ha

**日期**：2026-09-10（PT；本审复跑 ~01:17–01:18 PT）  
**审稿人**：`mw-e2e-ha`（对抗独立审；实现方不自审）  
**结论**：**pass**  
**是否允许 partial**：**是**（矩阵维持 **partial** + P1-2；**禁止**升 **covered**）  
**releaseEvidence=false** · **Not HA** · **≠ UC-E2E-011 covered** · **本绿 ≠ 全链路 E2E covered** · **R5 green-risk** · **NON-UI** · **无 Key**

## Prove（本审独立复跑）

| CMD | EXIT | 摘要 |
|-----|------|------|
| `pnpm uc011:report-refund:prove` | **0** | R1–R4 **全 PASS**；R5-MARKED-RED（`E2E_PG_IMAGE=pgvector`）；receipt `.tmp/isolated-proof-receipts/2026-09-10T08-17-56-981Z-764647-70988ace-a891-4277-a1f6-9634a903a93e.json`（`releaseEvidence=false` · `exitCode=0` · `target=uc011:report-refund:prove:raw`）；打印 `GAP-UC011-REFUND-CALLBACK` + `BLOCKED_FOR_FULL_E2E` |
| `pnpm eval-harness-matrix-cite:prove` | **0** | `uc-e2e-011-report-refund` harness+eval 引用矩阵行；钉 partial / ≠covered / releaseEvidence=false / Not HA / report-fail no-refund / interview-fail release / refund-callback GAP；**matrix: UC-E2E-011 is partial (not covered)** |

### R1–R4 核对（实测）

| ID | 声称 | 本审结果 |
|----|------|----------|
| **R1** | 面试失败→`failed`+`released`+额度净变 0；二次 fail 幂等 | **PASS**（reserve→reserved；5→4→5；二次 `already_failed`+`noop`；额度仍 5） |
| **R2** | complete+confirm 后 report quarantine：**不退**额度；interview 仍 completed；consumption 仍 confirmed；quarantined 不可 `requeueFailedReport` | **PASS**（attempts≥MAX=3；额度仍 4；regenerate→UC-019 BLOCKED 诚实钉） |
| **R3** | completed+confirmed 上 `releaseConsumption` → `error already_confirmed`；额度/状态不变 | **PASS**（无 confirmed→released） |
| **R4** | schema **无** `refunded` + 打印 `GAP-UC011-REFUND-CALLBACK`；EXIT=0 仅=诚实钉 **≠** 退款回调闭环 covered | **PASS**（CHECK 含 confirmed/released、不含 refunded；GAP 已印） |

## 硬钉（勾选）

- [x] **≠ covered**（prove 绿 ≠ UC-E2E-011 covered；cite 亦钉；矩阵行 **partial**）
- [x] **R5 green-risk**（isolated → pgvector；banner 已印；≠ sole-stack / HA）
- [x] **releaseEvidence=false** · Not HA
- [x] 未把 `commerce:prove` / `neg:commerce` / `report:prove` 冒充本 UC 专用验收
- [x] 未把 `full.e2e` `report_unavailable`+quarantined 兜底写成额度回滚已断言
- [x] **未把 R4「无退款回调」假写成退款闭环 covered**（见下）

## 假绿排查（优先：退款回调）

| 风险说法 | 裁定 |
|---------|------|
| `uc011:report-refund:prove` 绿 = **covered** | **否** — 最多 **partial**；缺支付退款回调 / HTTP 额度断言 / balance-ui / regenerate(UC-019) |
| R4 EXIT=0 = 退款幂等 / confirmed→refunded **已闭环 covered** | **否** — R4 是 **honesty GAP pin**：断言 schema **不含** `refunded` 并打印 `GAP-UC011-REFUND-CALLBACK`；明确「EXIT=0 仅=诚实钉，≠退款回调 covered」。旁证：`packages/db/src/payment.ts` 仅 `createOrder` / `getOrder` / `markOrderPaidAndCredit`，**无** refund API |
| `full.e2e` `report_unavailable` 绿 = 011 退款边界 covered | **否** — `e2e/full.e2e.ts` 有 quarantine 兜底；**缺**报告失败路径上的额度/`entitlement_consumption` 回滚业务断言（可用额度断言仅见 OCR 成功面，≠本 UC） |
| `commerce:prove` / `report:prove` 绿 = 011 关闭 | **否** — 另轨旁证 ≠ R1–R3 专用合同 |
| isolated/pgvector 绿 = sole-stack / HA / releaseEvidence | **否** — R5 mark-red；receipt `releaseEvidence=false` |
| 空断言 / stub harness / 矩阵假 covered | **否** — R1–R4 真 DB 断言；cite 钉 harness 非 stub、**does not claim covered**；矩阵 **partial** |

**partial 是否诚实**：**是**。db 集成 R1–R3 + R4 GAP pin 可执行合同已绿，足以支撑矩阵 **partial** + P1-2「集成 prove 已挂」；缺口仍明示，**未**把「无退款回调」写成闭环 covered，**未**假升 covered。

## 阻塞栏（升 covered / 全链路前必填）

| 阻塞项 | 现状 | 关闭条件 |
|--------|------|----------|
| 支付退款回调 / `confirmed→refunded` | `payment.ts` 无 refund API；consumption status CHECK **无** `refunded`；R4 仅 GAP pin | 产品路径落库 + TC-E2E-011-refund-idem 可执行断言（幂等）后复评 |
| HTTP / `full.e2e` / `e2e:isolated` 额度回滚断言 | `full.e2e` 有 `report_unavailable`+quarantined；**无**报告失败→额度/`ConsumptionRecord` 业务断言 | 显式 TC：面试 fail→released+净 0；报告 quarantine→confirmed 不退；进 isolated 复跑绿 |
| balance-ui | e2e UI only；本 NON-UI 切片未要求 | 产品接线后 UI E2E（降次于 HTTP） |
| regenerate 免费重生成（UC-E2E-019） | quarantined 上 `requeueFailedReport`=false 已钉 BLOCKED；矩阵 UC-019 **gap** | UC-019 产品口 + 与退款并发合同后再评；**禁止**借本绿冒充 019 |
| fixture=pgvector → R5 | isolated 仍 pgvector | 按 M5/sole-stack 迁关系 prove→MySQL 或持续 mark-red；**不因本绿宣称 HA** |

**本切片不因上述阻塞而 block partial**；上述仅 **阻塞升 covered / 宣称全链路 E2E covered / 退款回调闭环 covered**。

## 结论与建议

- **pass**；**允许并维持**矩阵 **partial**（P1-2）；**禁止 covered**
- D1 边界未被写反：面试失败→released（R1）；报告失败默认不退（R2）；confirmed 拒误 release（R3）
- R4 诚实：无退款回调 = GAP pin，**≠** 退款闭环 covered（假绿排查通过）
- 下一刀 P1：支付 `confirmed→refunded` / refund-idem；并行把额度回滚断言挂进 `full.e2e`/`e2e:isolated`
- 对照：`harness/uc-e2e-011-report-refund.md` · `eval/uc-e2e-011-report-refund.eval.md` · `packages/db/test/uc-e2e-011-report-refund.proof.ts` · 矩阵 `UC-E2E-011` / P1-2
