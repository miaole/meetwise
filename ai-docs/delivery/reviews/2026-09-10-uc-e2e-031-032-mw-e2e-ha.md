# 审查归档 — UC-E2E-031/032 注入越狱 / 诱导造假 · mw-e2e-ha（对抗独立审）

**日期**：2026-09-10（PT；本审独立复跑 ~02:18 PT）  
**审稿人**：`mw-e2e-ha`（对抗独立审；实现方不自审；**不**因 `mw-model-op` spot pass 放水）  
**结论**：**pass**（诚实 gap(e2e)/partial(eval) 登记）  
**是否允许 covered**：**否**  
**是否允许维持 gap(e2e)/partial(eval)**：**是**（honest；静态 inventory + GAP mark-red + 旁证 cite；**禁止**升 **covered** / 用 e2e fake-model 冒充安全闭环）  
**releaseEvidence=false** · **Not HA** · **≠ UC-E2E-031/032 covered** · **本绿 ≠ 全链路 E2E covered** · **R5 green-risk** · **NON-UI** · **无 Key** · **无 e2e fake-model jailbreak pass**

> EXIT=0 = S1–S6 静态诚实钉 + G-GAP×6 ≠ 真模型抗注入/不造假闭环 ≠ UC-E2E-031/032 **covered**。  
> **核心对抗钉**：质量断言归 **ai-eval**；e2e **禁止** `E2E_FAKE_MODEL` / graph-fake-model 冒充 model-resist / no-fabricate；抬 covered = **ai-eval suite + gates**（非 e2e）。  
> model-op spot（`2026-09-10-uc-e2e-031-032-mw-model-op-spot.md`）对本切片 **pass** **不改变**本审门槛：不得因 spot 放水升 covered / 宣称安全质量闭环 / 擦掉 gap(e2e)。

## Prove（本审独立复跑）

| CMD | EXIT | 摘要 |
|-----|------|------|
| `pnpm uc031-032:injection-jailbreak:prove` | **0** | S1–S6 + G-GAP **7 PASS**；印 GAPS=6：`GAP-UC031-E2E-STRUCTURE` / `GAP-UC031-AI-EVAL` / `GAP-UC032-FABRICATE-REJECT` / `GAP-UC032-AI-EVAL` / `GAP-UC031-032-FAKE-MODEL-BAN` / `GAP-UC031-032-GUARDRAIL`；`[R5-MARKED-RED]`（`E2E_PG_IMAGE=pgvector`）；receipt `.tmp/isolated-proof-receipts/2026-09-10T09-18-33-558Z-936677-96be8df9-6daf-460d-bdb2-40ada53cce58.json`（`releaseEvidence=false` · `exitCode=0` · `target=uc031-032:injection-jailbreak:prove:raw` · `outcome=passed`）；NOTE 明示 matrix=gap(e2e)/partial(eval) · EXIT=0=honesty pin ≠ fake jailbreak pass · 抬 covered=ai-eval |
| `pnpm eval-harness-matrix-cite:prove` | **0** | `uc-e2e-031-032-injection-jailbreak` harness+eval 引用 `UC-E2E-031`/`032`；钉 gap+partial / ≠covered / releaseEvidence=false / Not HA / jailbreak·fabrication / GAP 031·032 ai-eval / **fake-model ban** / golden-tasks 旁证 / R5 / **covered-path = ai-eval**；**matrix: UC-E2E-031/032 remains gap(e2e)/partial(eval)**；`does not claim covered` |
| golden-tasks / scoring:eval / TC-*-jailbreak | 旁证（未作本 UC 验收） | 旁证 ≠ UC-E2E-031/032 covered |

### S1–S6 + G-GAP 核对（实测 + 搜码对抗）

| ID | 声称 | 本审结果 |
|----|------|----------|
| **S1** | scenarios 层分流：031/032 model-resist / no-fabricate → **ai-eval**；禁 e2e fake-model 证抵抗 | **PASS**（scenarios 钉「移出 e2e → ai-eval」；fake-model 无法证明模型抵抗） |
| **S2** | `e2e/*` / `full.e2e` **无** UC/TC-031/032；无 fake-model jailbreak/造假 pass；无 model-resist 质量断言 | **PASS**（搜码 **NO_E2E_031_032_HIT**） |
| **S3** | golden-tasks / scoring:eval / TC-AIIV-002 / TC-RES-012 / TC-quiz-078 / safety-defense-in-depth 旁证路径；cite ≠ covered | **PASS** |
| **S4** | `E2E_FAKE_MODEL` fail-closed（flags + static-guards + honesty-rules） | **PASS**（`assertNoFakeServiceFlags` · `fake_service_mode_forbidden`） |
| **S5** | 矩阵保持 **gap**(e2e) / **partial**(eval)；不得 **covered** | **PASS**（矩阵行 + cite 硬钉） |
| **S6** | GuardrailHit：status-machine 有对象；无 `guardrail_hit`/`guard_events` CREATE | **PASS**（inventory unwired） |
| **G-GAP** | 印齐 6 PIN；若 e2e 用 fake-model 宣称 031/032 安全闭环 → 拒 EXIT=0 | **PASS**（6 pins；≠闭环；fail-closed 于假绿浮出） |

**搜码对抗（禁 e2e fake-model 冒充安全闭环）**  
- `e2e/`：**无** `UC-E2E-031`/`032` / `TC-E2E-031`/`032` / `model-resist` / `no-fabricate` 命中 → 与 S2 / `GAP-UC031-032-FAKE-MODEL-BAN` 一致。  
- scenarios：质量断言明确 **移出 e2e → ai-eval**；fake-model fixture 无法证明抗注入/不造假。  
- `scripts/e2e-fake-service-flags.mjs` + `e2e-static-guards.mjs`：`E2E_FAKE_MODEL` fail-closed。  
- quiz `TC-quiz-078-jailbreak` graph-fake-model = **机制旁证 ≠** 生产模型抵抗率（S3 边界）。  
- prove 为静态 inventory；**无** HTTP 假越狱通过、**无** `MODEL_API_KEY`、**无** e2e fake-model pass。

## 硬钉（勾选）

- [x] **矩阵保持 gap(e2e) / partial(eval)**（行 `UC-E2E-031 / 032` + P1-12）；未升 **covered**
- [x] EXIT=0 = honesty pin（S1–S6 + G-GAP×6），**非**安全质量闭环 / **非** fake jailbreak pass
- [x] 质量断言归 **ai-eval**；抬 covered 主轨 = **ai-eval suite + gates**（harness §1b）；**禁止** e2e fake-model
- [x] 未把 golden-tasks / scoring:eval / TC-quiz-078 graph-fake-model / TC-AIIV-002 / TC-RES-012 冒充本 UC covered
- [x] 钉 **R5 green-risk**（isolated → pgvector）；本绿 ≠ 全链路 E2E covered
- [x] `releaseEvidence=false` · Not HA（receipt + banner）
- [x] G-GAP EXIT=0 读作诚实钉，非抗注入/不造假已闭环
- [x] **未因 mw-model-op spot pass 放水**（独立复跑一致；仍 gap(e2e)/partial(eval)；仍 ≠ covered）

## 假绿排查（优先：e2e fake-model / 旁证冒充 covered）

| 风险说法 | 裁定 |
|---------|------|
| `uc031-032:…:prove` 绿 = **UC-E2E-031/032 covered** / 「抗注入/不造假已通」 | **否** — EXIT=0 = honesty pin；矩阵保持 **gap**(e2e)/**partial**(eval) |
| e2e + `E2E_FAKE_MODEL` 绿 = 模型抗注入 covered | **否** — **假绿**；fake-model 返回 fixture 无论是否注入；`GAP-UC031-032-FAKE-MODEL-BAN`；本 prove fail-closed |
| `TC-quiz-078` graph-fake-model = UC-031 covered | **否** — 机制路由拒绝 ≠ 生产模型抵抗率；旁证 ≠ covered |
| `scoring:eval` / `golden-tasks:check` 绿 = 031/032 covered | **否** — 旁证/登记门 ≠ 本行安全质量闭环 |
| G-GAP EXIT=0 = 抗注入已闭环 / AI-EVAL 已齐 | **否** — GAP 表示质量归 ai-eval、e2e 结构仍缺 |
| mem07 / research-policy deny = 模型不越狱 | **否** — 域围栏 ≠ model-resist ai-eval |
| isolated/pgvector 绿 = sole-stack / HA / releaseEvidence | **否** — R5 mark-red；receipt `releaseEvidence=false` |
| model-op spot pass → 可升 covered / 擦掉 gap(e2e) | **否** — spot 亦钉 gap/partial ≠ covered；本审独立复跑，**不放水** |
| cite 对部分 UC 接受 partial → 031/032 可偷升 covered | **否** — cite 硬钉 `remains gap(e2e)/partial(eval)`；本次未偷升 |

**gap(e2e)/partial(eval) 是否诚实**：**是**。S1–S6 可执行证明「层分流→ai-eval · e2e 无假绿 · 旁证路径 · E2E_FAKE_MODEL 禁令 · 矩阵状态 · GuardrailHit 未接线」+ G-GAP×6 明示结构/ai-eval/造假拒绝/假模型禁/护栏缺口；矩阵行与 P1-12 均为 **gap**(e2e)/**partial**(eval)；文档反复钉 ≠ covered / 禁 e2e fake-model；**未**把旁证或 prove 绿写成安全质量闭环；partial(eval) 仅=旁证/分流库存，**不是**真模型 eval suite 已绿。

## 阻塞栏（升 covered / 宣称安全闭环 / 全链路前必填）

| 阻塞项 | 现状 | 关闭条件 |
|--------|------|----------|
| 031 真模型抗注入 ai-eval suite + release-gate | `GAP-UC031-AI-EVAL`；无生产模型 jailbreak golden+阈值门 | 固定版本 jailbreak/injection golden（规模·分层·阈值·owner）挂入 release-gate；真模型跑 `TC-E2E-031-model-resist` 后复评 |
| 032 真模型不造假 ai-eval suite + over-refusal 反例 | `GAP-UC032-AI-EVAL` | factuality / no-fabricate golden + `TC-E2E-032-no-fabricate-model` + 阈值门后复评 |
| 与既有旁证对齐但不冒充（AIIV/RES/safety 桶） | S3 旁证库存有；≠ covered | safety-defense-in-depth 桶 release-gate 谓词 + 真模型门；**禁止**借 TC-quiz-078 冒充 |
| `scoring:eval` / golden-tasks 可判定安全指标 | 旁证路径存在；inconclusive | 拦截率下限/绕过率上限/最小样本量可判定；安全类条目最高 partial，禁 fake mapped |
| e2e **仅**结构：DOM 转义 / biz-reject | `GAP-UC031-E2E-STRUCTURE` / `GAP-UC032-FABRICATE-REJECT`；e2e 仍 0 | isolated 结构 TC（escape / biz-reject）；**不得**把分数「没被拉满」写成模型抗注入 |
| GuardrailHit append-only 表+API+审计 | `GAP-UC031-032-GUARDRAIL`；runtime 表=0 | GuardrailHit(injection/jailbreak/fabrication) 产品接线 + prove 后复评 |
| 禁止路径：e2e fake-model 宣称 covered | `GAP-UC031-032-FAKE-MODEL-BAN`；当前无浮出 | **持续 fail-closed**；任何 `E2E_FAKE_MODEL`/graph-fake-model 绿条宣称 031/032 covered → 拒 |
| fixture=pgvector → R5 | isolated 仍 pgvector | sole-stack 迁出后才可讨论发布级 covered；**不因本绿宣称 HA** |

**本切片不因上述阻塞而 block 维持 gap(e2e)/partial(eval)**；上述仅 **阻塞升 covered / 宣称安全质量闭环 / 用 e2e fake-model 或旁证冒充抗注入·不造假 / 宣称全链路 E2E covered / HA**。

## 结论与建议

- **pass**；**允许并维持**矩阵 **gap**(e2e) / **partial**(eval)（P1-12）；**禁止 covered**
- 诚实钉成立：层分流→ai-eval · e2e 无 031/032 假绿 · E2E_FAKE_MODEL fail-closed · G-GAP×6 齐 · cite 硬钉 gap/partial
- **硬禁通过**：未发现 e2e fake-model 冒充安全闭环；ai-eval 边界诚实（partial(eval)=旁证库存 ≠ 真模型 suite 已绿）
- **不因 model-op spot 放水**：独立复跑 EXIT=0 一致；裁定不变
- 下一刀优先：**ai-eval jailbreak/no-fabricate golden + 阈值门**（抬 covered 主轨）；并行结构 e2e escape/biz-reject（质量仍不得进 e2e）；GuardrailHit 产品接线另轨
- 对照：`harness/uc-e2e-031-032-injection-jailbreak.md` · `eval/uc-e2e-031-032-injection-jailbreak.eval.md` · `apps/api/test/uc-e2e-031-032-injection-jailbreak.proof.mjs` · 矩阵 `UC-E2E-031 / 032` / P1-12 · spot `reviews/2026-09-10-uc-e2e-031-032-mw-model-op-spot.md`
