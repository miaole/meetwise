# 审查归档 — UC-E2E-033 越权（C 跨用户 / B-C）· mw-e2e-ha

**日期**：2026-09-10（PT；本审独立复跑 ~01:47 PT）  
**审稿人**：`mw-e2e-ha`（对抗独立审；实现方不自审）  
**结论**：**pass**  
**是否允许 partial**：**是**（矩阵维持 **partial** + P1-7；**禁止**升 **covered**）  
**releaseEvidence=false** · **Not HA** · **≠ UC-E2E-033 covered** · **本绿 ≠ 全链路 E2E covered** · **R5 green-risk** · **NON-UI** · **无 Key** · **系统化七类未齐**

> privacy-int spot（`2026-09-10-uc-e2e-033-mw-privacy-int-spot.md`）对本 UC 跨用户 404 / export 隔离 **pass** **不改变**本审门槛：不得因 spot 放水升 covered / 宣称七类齐 / A3 worker 闭环 / 全链路 E2E covered。

## Prove（本审独立复跑）

| CMD | EXIT | 摘要 |
|-----|------|------|
| `pnpm uc033:cross-user-authz:prove` | **0** | X1–X8 + G-GAP **33 PASS**；`[R5-MARKED-RED]`（`E2E_PG_IMAGE=pgvector`）；receipt `.tmp/isolated-proof-receipts/2026-09-10T08-47-23-119Z-848550-a9f78291-99eb-4d6c-b109-30546700e8c3.json`（`releaseEvidence=false` · `exitCode=0` · `target=uc033:cross-user-authz:prove:raw` · `outcome=passed`）；印 GAP-UC033-WORKER-PRINCIPAL / CACHE-TRACE / SEVEN-CLASS / FULL-E2E / NEG-CITE |
| `pnpm eval-harness-matrix-cite:prove` | **0** | `uc-e2e-033-cross-user-authz` harness+eval 引用矩阵行；钉 partial / ≠covered / releaseEvidence=false / Not HA / cross-user·B-C / seven-class GAP / neg:*/full.e2e 旁证 / R5；**matrix: UC-E2E-033 is partial (not covered)**；`does not claim covered` |

### X1–X8 + G-GAP 核对（实测）

| ID | 声称 | 本审结果 |
|----|------|----------|
| **X1** | C-cross 面试 begin/turn/assessment/report/events/transcript/abandon → 404 | **PASS**（7） |
| **X2** | C-cross 简历 list 过滤 + profile/reparse 404；对照 owner 可见 | **PASS**（4） |
| **X3** | C-cross 订单 GET ORD_A → 404 `not_found`（含 token） | **PASS**（2） |
| **X4** | C-cross quiz/diagnosis GET/begin → 404 | **PASS**（4） |
| **X5** | B-C：recU 读 C 面试/简历 → 404；candidate→`/recruiter/*` → 403 `recruiter_required` | **PASS**（4） |
| **X6** | candidate/recruiter → `/admin/*` → 403 `admin_required` | **PASS**（2） |
| **X7** | A2 无/错 principal → interview/resume/quiz **0 行**；对照 owner=1 | **PASS**（5） |
| **X8** | `GET /privacy/export` userB 不含 A interview/resume；对照 A 含 | **PASS**（4） |
| **G-GAP** | 打印 GAP-UC033-*（WORKER / CACHE-TRACE / SEVEN-CLASS / FULL-E2E / NEG-CITE）；EXIT=0 仅=诚实钉 | **PASS**（1；印齐 5 PIN；≠闭环） |

## 硬钉（勾选）

- [x] **≠ covered**（prove 绿 ≠ UC-E2E-033 covered；cite 亦钉；矩阵行 **partial**）
- [x] **R5 green-risk**（isolated → pgvector；banner 已印；≠ sole-stack / HA）
- [x] **releaseEvidence=false** · Not HA
- [x] 未把 `neg:auth` / `neg:bend` / `neg:interview` / `neg:commerce` / `full.e2e` B RLS 冒充本 UC **covered**（旁证 ≠ covered）
- [x] 未把 G-GAP EXIT=0 写成七类已齐 / A3 worker principal 闭环 / full.e2e UC033 场景已挂
- [x] 未因 privacy-int spot pass 放水（export 隔离 + 404 ≠ 擦除闭环 / ≠ RLS 全量 / ≠ covered）

## 假绿排查（优先：旁证冒充 + GAP 诚实）

| 风险说法 | 裁定 |
|---------|------|
| `uc033:cross-user-authz:prove` 绿 = **covered** | **否** — 最多 **partial**；七类未齐；A3 worker / cache-trace / full.e2e 独立场景仍 GAP |
| `neg:auth` / `neg:bend` / `neg:interview` / `neg:commerce` 绿 = 033 covered | **否** — 旁证子集；harness/eval/cite/G-GAP-NEG-CITE 均钉 ≠ 本 UC 系统化七类 |
| `full.e2e` B RLS 绿 = 033 covered | **否** — B 端岗位隔离旁证 ≠ C-cross+B-C+A2+A3 全矩阵；`GAP-UC033-FULL-E2E` |
| G-GAP EXIT=0 = 七类已齐 / A3 worker 闭环 | **否** — 诚实钉；prove 明确「EXIT=0≠covered；七类未齐」 |
| `tenant-enforcement:prove` 绿 = UC-033 covered | **否** — 应用层 tenant ≠ RLS / ≠本 UC（harness 假绿表） |
| isolated/pgvector 绿 = sole-stack / HA / releaseEvidence | **否** — R5 mark-red；receipt `releaseEvidence=false` |
| privacy-int spot pass → 可升 covered / 七类齐 / 删除闭环 | **否** — spot 不抬门槛；本 prove 仅 HTTP/DB 合同 + export 隔离；schema additive stub 服务 404 路径 |

**partial 是否诚实**：**是**。X1–X8 HTTP/DB（C-cross 面试/简历/交易/押题诊断 + B-C + admin/recruiter 门禁 + A2 0 行 + export 隔离）+ G-GAP 诚实钉可执行合同已绿，足以支撑矩阵 **partial** + P1-7「HTTP/DB prove 已挂」；缺口（worker/cache/七类/full.e2e）明示，**未**把 neg:*/full.e2e RLS 写成业务 E2E covered，**未**假升 covered。

## 阻塞栏（升 covered / 全链路前必填）

| 阻塞项 | 现状 | 关闭条件 |
|--------|------|----------|
| A3 worker / checkpointer 路径带 principal | `GAP-UC033-WORKER-PRINCIPAL`；本 HTTP prove 不碰 worker 注入 | worker/job/checkpointer 可执行 principal 注入断言复跑绿后复评 |
| 缓存键 / trace / 批 job 全路径注入 | `GAP-UC033-CACHE-TRACE` | 缓存/trace/批 job 越权合同可执行后复评 |
| 系统化七类（含高并发越权竞态等） | `GAP-UC033-SEVEN-CLASS`；X1–X8 ≠ 七类齐 | 七类可执行合同齐备并绿后才可讨论更高覆盖（仍未必 covered） |
| `full.e2e` / `e2e:isolated` 独立 UC033 场景 | `GAP-UC033-FULL-E2E`；full.e2e B RLS=旁证≠covered | 独立 UC033 场景进 isolated 复跑绿后复评 |
| `neg:*` 旁证冒充本 UC covered | `GAP-UC033-NEG-CITE`；cite 已钉旁证 | 保持旁证降级；**禁止**借 neg 绿宣称本 UC covered |
| fixture=pgvector → R5 | isolated 仍 pgvector | 按 M5/sole-stack 迁关系 prove→MySQL 或持续 mark-red；**不因本绿宣称 HA** |
| privacy 擦除 / DELETE 闭环 | spot 仅 export 隔离；DELETE 另轨须 503 | **禁止**借本绿/spot 宣称隐私删除闭环或放弃 RLS |

**本切片不因上述阻塞而 block partial**；上述仅 **阻塞升 covered / 宣称全链路 E2E covered / 把 neg:*/full.e2e 旁证或 privacy spot 写成业务闭环 / 宣称七类已齐**。

## 结论与建议

- **pass**；**允许并维持**矩阵 **partial**（P1-7）；**禁止 covered**
- HTTP/DB 合同诚实：C-cross 404 + B-C 隔离 + 角色门禁 403 + A2 0 行 + export 仅己；GAP pins 齐
- 旁证 cite 正确降级（neg:*/full.e2e RLS ≠ covered）；假绿排查通过；**不因 privacy-int spot 放水**
- 下一刀 P1：worker principal 注入；并行七类高并发 / cache-trace；再议 full.e2e 独立 UC033
- 对照：`harness/uc-e2e-033-cross-user-authz.md` · `eval/uc-e2e-033-cross-user-authz.eval.md` · `apps/api/test/uc-e2e-033-cross-user-authz.proof.ts` · 矩阵 `UC-E2E-033` / P1-7
