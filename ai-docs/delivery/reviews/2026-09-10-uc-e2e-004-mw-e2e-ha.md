# 审查归档 — UC-E2E-004 career-path 全链路 · mw-e2e-ha（对抗独立审）

**日期**：2026-09-10（PT；本审独立复跑 ~02:01 PT）  
**审稿人**：`mw-e2e-ha`（对抗独立审；实现方不自审；**不因** `mw-rag-route` spot pass 放水）  
**结论**：**pass**（诚实 gap 登记）  
**是否允许 partial**：**否**（本切片仅静态 inventory + G-GAP mark-red；**矩阵必须维持 gap**；**禁止**假 partial-closed / covered）  
**releaseEvidence=false** · **Not HA** · **≠ UC-E2E-004 covered** · **本绿 ≠ 全链路 E2E covered** · **R5 green-risk** · **NON-UI** · **无 MODEL_API_KEY**

> EXIT=0 = S1–S5 库存绿 + G-GAP×5 诚实钉 ≠ A1/A2/A3 产品/E2E 闭环 ≠ TC-E2E-004-* 已绿。  
> 诚实钉死：HTTP=`sync deriveCareerPath` **≠** `AiGraphRun(career-path)`；无 GrowthTimeline 写；无 e2e TC-004。  
> 旁证（`neg:interview` / `scor-00-honesty` / `report:prove` / `graph:prove` / `validate.ts` / web report GET）**≠ covered**。  
> 对照 spot：`reviews/2026-09-10-uc-e2e-004-mw-rag-route-spot.md`（pass）— 本审独立复跑，**不**借 spot 升格。

## Prove（本审独立复跑）

| CMD | EXIT | 摘要 |
|-----|------|------|
| `pnpm uc004:career-path:prove` | **0** | S1–S5 + G-GAP **6 PASS**（含 `G-GAP-product-surface-or-pins`）；印 `GAP-UC004-E2E-MAIN` / `GRAPH` / `GROWTH-A1A2` / `FAIL-A3` / `UNCERTAINTY`（**GAPS=5**）；`[R5-MARKED-RED]`（`E2E_PG_IMAGE=pgvector`）；receipt `.tmp/isolated-proof-receipts/2026-09-10T09-01-09-164Z-880393-47063a9c-5419-495f-a535-27b184ad8da5.json`（`releaseEvidence=false` · `exitCode=0` · `target=uc004:career-path:prove:raw` · `outcome=passed`） |
| `pnpm eval-harness-matrix-cite:prove` | **0** | `uc-e2e-004-career-path` harness+eval 引用矩阵行；钉 **gap** / ≠covered / releaseEvidence=false / Not HA / career-path / TC·GAP e2e-main·graph·growth / R5 / 旁证；**matrix: UC-E2E-004 remains gap**；`does not claim covered` |
| `pnpm report:prove` / `neg:interview` / domain derive | 旁证（未作本 UC 验收） | 他图 / 负例 / 域确定性派生 ≠ career-path 全链路 |

### S1–S5 + G-GAP 核对（实测 + 搜码 spot）

| ID | 声称 | 本审结果 |
|----|------|----------|
| **S1** | controller POST/GET `:id/career-path`；service `generateCareerPath` / `getCareerPath` | **PASS**（controller L229/L235；service 签名在） |
| **S2** | `generateCareerPath` 调 `deriveCareerPath` + INSERT `career_path`；**无** AiGraphRun/worker | **PASS**（service ~749–770：`deriveCareerPath(overall, weaknesses)` + INSERT；无 graph wire） |
| **S3** | `packages/ai-graphs/src` **无** career-path 文件；index 不 export；report 旁证在 | **PASS**（`ls` → **NO_CAREER_GRAPH**；index 有 report export） |
| **S4** | generate **不**写 CapabilityProfile/GrowthTimeline；`profile.growth` **不** join `career_path` | **PASS**（generate 区无 GrowthTimeline；growth 读 `assessment_report` only） |
| **S5** | `e2e/*.e2e.ts` / `full.e2e.ts` **无** UC-E2E-004 / career-path 覆盖声称 | **PASS**（`rg` → **NO_E2E_HITS**） |
| **G-GAP** | 产品面未浮出 → 印齐 5 PIN；若图/GrowthTimeline 接线则拒 EXIT=0 | **PASS**（5 pins；≠闭环） |

**注释对抗**：service 注释写「成长链终点」——仅为叙述；实现仍只写 `career_path` 表、**不**更新 GrowthTimeline/CapabilityProfile；G-GAP-3 已诚实钉死，**不得**借注释冒充 A2 闭环。

## 硬钉（勾选）

- [x] **矩阵保持 gap**（行 `UC-E2E-004` + §3 **P1-9**）；未升 **covered**；未假 **partial**-closed
- [x] EXIT=0 = GAP mark-red（E2E-MAIN / GRAPH / GROWTH-A1A2 / FAIL-A3 / UNCERTAINTY），**非** A1/A2/A3 产品闭环
- [x] 钉 **R5 green-risk**；本绿 ≠ 全链路 E2E covered；`releaseEvidence=false` · Not HA
- [x] 诚实缺口钉死：HTTP=sync `deriveCareerPath` ≠ AiGraphRun；无 growth timeline 写；无 e2e TC-004
- [x] harness **§1b「抬到 covered 还缺」非空**（8 条 HTTP/product 路径，非仅 GAP 标签）
- [x] `report:prove` / `neg:interview` / domain derive / `validate.ts` / web report GET 标旁证 ≠ covered
- [x] 未因 `mw-rag-route` spot pass 放水 / 偷偷改 partial

## 假绿排查（优先：mark-red 绿冒充 partial/covered + GAP 诚实）

| 风险说法 | 裁定 |
|---------|------|
| `uc004:career-path:prove` 绿 = **UC-E2E-004 covered** | **否** — 静态 inventory + GAP pin；A1/A2/A3 / TC-E2E-004-* 未接线 |
| prove 绿 = **partial**-closed / 可改矩阵 partial | **否** — harness/eval/P1-9 明示保持 **gap**；mark-red/静态绿 **≠** 假 partial；升 partial 须至少 HTTP e2e 主路径或书面 ADR 降级承认 derive≠AiGraphRun 后再议；**本审裁定：不可因 mark-red EXIT=0 改 partial** |
| G-GAP EXIT=0 = A1/A2/A3 已闭环 | **否** — GAP 表示缺席；EXIT=0 仅=诚实钉 |
| `report:prove` / `graph:prove` 绿 = 004 covered | **否** — 他图 / report 舱壁 ≠ career-path 全链路 |
| `neg:interview` career-path 409 绿 = 004 | **否** — 前置负例 ≠ A1/A2/A3 E2E |
| `validate.ts` POST career-path 200 = covered | **否** — 局部 validate ≠ 隔离 e2e 全链 / 成长落点 |
| `deriveCareerPath` / `scor-00-honesty` 绿 = 图已通 | **否** — 域确定性派生 ≠ AiGraphRun(career-path) |
| web report 页 milestones 渲染 = 004 | **否** — GET 旁证 ≠ 生成全链 / 成长关联 / 失败降级 |
| isolated/pgvector 绿 = sole-stack / HA / releaseEvidence | **否** — R5 mark-red；receipt `releaseEvidence=false` |
| cite EXIT=0 = 业务 covered | **否** — cite 仅 harness/eval/矩阵引用与 **gap** 钉 |
| cite 对 004 亦接受 partial 软分支 → 可偷偷改 partial | **否（本次未发生）** — `eval-harness-matrix-cite.proof.mjs` 对 UC-E2E-004 存在 `**partial**`→pass 软分支（与 025 同类）；但矩阵行实测仍为 `**gap**`，cite 印 **remains gap**；harness/eval/P1-9 均禁假 partial；若文档无执行体偷升 partial → **conditional/block**（本审对抗钉死 **gap**，未放水） |
| rag-route spot pass → 可放宽矩阵 / 升 partial | **否** — spot 仅确认未冒充 graph/成长/RAG 闭环；**不**改变 gap 裁定 |

**gap 是否诚实**：**是**。S1–S5 可执行证明「HTTP=derive、无图、无 GrowthTimeline 写、无 e2e」+ G-GAP×5 明示 E2E-MAIN/GRAPH/GROWTH/FAIL-A3/UNCERTAINTY；矩阵行与 P1-9 均为 **gap**；§1b 抬 covered 还缺非空；文档反复钉 ≠ covered / 禁假 partial-closed；**未**把旁证或静态绿写成业务闭环；**未**偷偷改 partial。

## 阻塞栏（升 partial / covered / 全链路前必填）

| 阻塞项 | 现状 | 关闭条件 |
|--------|------|----------|
| e2e HTTP 主路径（TC-E2E-004-main / A1·A2） | `GAP-UC004-E2E-MAIN`；`e2e/*` **无** career-path / UC-004 | 隔离 HTTP：鉴权→有评估→POST/GET career-path 确定字段可执行并复跑绿后，才可讨论 **partial** |
| AiGraphRun(career-path) 或书面 ADR 降级 | `GAP-UC004-GRAPH`；无图文件；HTTP=sync derive | 落 career-path LangGraph + worker prove，**或** ADR 正式承认 HTTP=derive 并改写 UC 验收后复评 |
| CapabilityProfile / GrowthTimeline 写入 + 档案关联（A2） | `GAP-UC004-GROWTH-A1A2`；generate 只写 `career_path`；growth 只聚合 `assessment_report` | 成功路径更新/关联成长档案确定字段 + `GET /profile/growth`（或等价）可断言后复评 |
| 能力曲线 ≥2 维确定字段（A1） | 未进 e2e；milestones 非空 ≠ ≥2 维曲线 | e2e/HTTP 对 ≥2 维数据点断言后复评 |
| 图失败降级 + 重试 + 额度净变 0（A3 / TC-fail） | `GAP-UC004-FAIL-A3` | `AiGraphRun=failed` + 可见降级 + 重试 + 额度断言 HTTP/E2E 绿后复评 |
| 不确定性双校验拒收（TC-uncertainty） | `GAP-UC004-UNCERTAINTY`；质量归 ai-eval | 产品闸 HTTP 可证（措辞仍归 ai-eval）后复评 |
| UI 成长档案「生成职业路径」触发/渲染 | harness §1b #7；次层 | `e2e:ui:isolated` 可证；**不可单独**升 covered |
| fixture=pgvector → R5 | isolated 仍 pgvector；banner 已印 | sole-stack 夹具替换或持续 mark-red；**不因本绿宣称 HA / releaseEvidence** |
| 旁证冒充本 UC | report/neg/domain/validate/web GET | 保持旁证降级；**禁止**借旁证绿宣称 004 covered/partial |
| cite 软分支接受 partial | cite 脚本对 004 有 `**partial**`→pass 分支 | 矩阵/文档须保持 **gap** 直至产品路径齐；偷升 partial 则本审改 **conditional/block** |

**本切片不因上述阻塞而 block「诚实 gap 登记」pass**；上述 **阻塞升 partial / covered / 宣称 A1·A2·A3 闭环 / 把 mark-red 绿写成 partial-closed / 宣称全链路 E2E covered**。

## 结论与建议

- **pass**；**矩阵裁定：维持 gap**（honest mark-red）；**禁止 covered**；**禁止**因本绿 / rag spot 改 **partial**
- 静态诚实面成立：HTTP=sync `deriveCareerPath`≠AiGraphRun；无 career-path 图；无 GrowthTimeline 写；无 e2e TC-004；G-GAP 五钉齐；§1b 非空
- 假绿排查通过；cite 钉 `UC-E2E-004 remains gap`；receipt `releaseEvidence=false`；R5 已钉
- 下一刀（升 partial 门槛，二选一优先）：**(a)** ADR 书面降级承认 derive≠AiGraphRun 并改写验收，或 **(b)** e2e HTTP 主路径 + 成长落点；在此之前矩阵 **必须 gap**
- 对照：`harness/uc-e2e-004-career-path.md` · `eval/uc-e2e-004-career-path.eval.md` · `apps/api/test/uc-e2e-004-career-path.proof.mjs` · 矩阵 `UC-E2E-004` / §3 **P1-9** · spot `reviews/2026-09-10-uc-e2e-004-mw-rag-route-spot.md`
