# 审查归档 — UC-E2E-027 人工复核申诉 · mw-e2e-ha（对抗独立审）

**日期**：2026-09-10（PT；本审独立复跑 ~02:13–02:14 PT）  
**审稿人**：`mw-e2e-ha`（对抗独立审；实现方不自审；**不**因 `meetwise honesty` spot pass 放水）  
**结论**：**pass**（诚实 gap/blocked 登记）  
**是否允许 partial**：**否**（本切片仅静态 inventory + GAP mark-red；**矩阵必须维持 gap/blocked**；**禁止**假 partial-closed / covered）  
**releaseEvidence=false** · **Not HA** · **≠ UC-E2E-027 covered** · **本绿 ≠ 全链路 E2E covered** · **R5 green-risk** · **NON-UI** · **无 Key** · **无 fake API**

> EXIT=0 = S1–S5 库存 + G-GAP×6 诚实钉 ≠ A1 appeal open / A2 overturn CAS / A3 幂等 / A4 lease effect=0 产品/E2E 闭环 ≠ TC-E2E-027-* 已绿。  
> **核心对抗钉**：产品面 **未接线**（无 ManualReview 模块/HTTP/表/e2e）；EXIT=0 **不得**读成「申诉已通 / 已接线 / partial」。  
> qbank `ReviewDecision` / `SelectiveReviewDecision` / resume `needs_review` / web「人工复核还没开放」= **旁证 ≠ covered**。

## Prove（本审独立复跑）

| CMD | EXIT | 摘要 |
|-----|------|------|
| `pnpm uc027:manual-review-appeal:prove` | **0** | S1–S5 + G-GAP **6 PASS**（含 G-GAP-product-surface-or-pins）；印 `GAP-UC027-APPEAL-OPEN` / `OVERTURN-CAS` / `IDEMPOTENT` / `LEASE-EFFECT0` / `D3-STATUS` / `E2E`（GAPS=6）；`[R5-MARKED-RED]`（`E2E_PG_IMAGE=pgvector`）；receipt `.tmp/isolated-proof-receipts/2026-09-10T09-13-35-340Z-920387-1f13813a-5077-4d17-97d4-81bc2de38028.json`（`releaseEvidence=false` · `exitCode=0` · `target=uc027:manual-review-appeal:prove:raw` · `outcome=passed`） |
| `pnpm eval-harness-matrix-cite:prove` | **0** | `uc-e2e-027-manual-review-appeal` harness+eval 引用矩阵行；钉 **gap** + **blocked** / ≠covered / releaseEvidence=false / Not HA / ManualReview·appeal / GAP APPEAL-OPEN·OVERTURN / qbank 旁证 / R5 / **no fake API**；**matrix: UC-E2E-027 remains gap/blocked**；`does not claim covered` |
| qbank / SelectiveReview / needs_review / web 文案 | 旁证（未作本 UC 验收） | 题库审批 / 评分路由 / OCR 待审 / 「还没开放」≠ ManualReview 申诉闭环 |

### S1–S5 + G-GAP 核对（实测 + 搜码 spot）

| ID | 声称 | 本审结果 |
|----|------|----------|
| **S1** | 无 `appeal` / `manual-review` / `human-review` 模块与 controller 路由 | **PASS**（`apps/api/src/modules` 无对应目录；controller 无 appeal/manual-review 路由命中） |
| **S2** | migrations 无 ManualReview 域表 CREATE | **PASS**（无 `manual_review` / `review_decision` / `review_effect` / `review_assignment` / evidence|audit CREATE） |
| **S3** | qbank/Selective/needs_review 旁证边界；不宣称 UC-027 | **PASS** |
| **S4** | `e2e/*.ts` / `full.e2e` 无 UC-E2E-027 / TC-E2E-027 | **PASS**（搜码 NO_E2E_HIT） |
| **S5** | scenarios 定义 027；status-machine + human-review-design **TARGET**；web「还没开放」 | **PASS**（`### ManualReview（…TARGET）`；design「运行时…均为 **0**」；how-it-works「校准和人工复核工单还没开放」） |
| **G-GAP** | 产品面未浮出 → 印齐 6 PIN；若路由/表/模块/e2e 浮出则拒 EXIT=0 | **PASS**（6 pins；≠闭环；fail-closed 于产品浮出） |

**搜码对抗（产品未接线 · 禁止误读为已接线/partial）**  
- 模块 / 路由 / 表 / e2e：均为 **0 命中** → 与 G-GAP「product blocked / unwired」一致。  
- `status-machine.md`：ManualReview = **TARGET**；「当前没有运行时表/API/UI」。  
- `human-review-design.md`：评分申诉/改判「运行时代码中通用 `ManualReview` 表、API、审核员角色、案件 UI、申诉 E2E 均为 **0**」。  
- web 文案「还没开放」= honesty，**非**产品闭环证据。  
- prove 无 HTTP 调用、无 fake appeal handler、无 MODEL_API_KEY — **无假 API**。

## 硬钉（勾选）

- [x] **矩阵保持 gap / blocked**（行 `UC-E2E-027` + P1-11）；未升 **covered**；未假 **partial**-closed
- [x] EXIT=0 = GAP mark-red（APPEAL-OPEN / OVERTURN-CAS / IDEMPOTENT / LEASE-EFFECT0 / D3-STATUS / E2E），**非** A1–A4 产品闭环
- [x] 钉 **产品未接线**（模块/HTTP/表/e2e=0）；EXIT=0 ≠ 已接线 / ≠ partial
- [x] harness **§1b**「抬到 covered 还缺」已列 HTTP/product 路径（非仅 GAP 标签）
- [x] 钉 **R5 green-risk**；本绿 ≠ 全链路 E2E covered
- [x] qbank / SelectiveReview / needs_review / web 文案标为旁证 ≠ covered
- [x] `releaseEvidence=false` · Not HA（receipt + banner）
- [x] **无 fake API**；未假装申诉闭环
- [x] 未因 `meetwise honesty` spot 已 pass 而跳过独立复跑 / 放水升档

## 假绿排查（优先：EXIT=0 冒充产品已接线 / partial）

| 风险说法 | 裁定 |
|---------|------|
| `uc027:…:prove` 绿 = **covered** / 「人工申诉复核已通」 | **否** — EXIT=0 = honest gap/blocked mark-red；产品面仍 0 |
| prove 绿 = **partial**-closed / 可改矩阵 partial | **否** — harness/eval/P1-11/cite 明示保持 **gap/blocked**；本切片 **零** HTTP/表实现面，**不可**因 mark-red EXIT=0 改 partial |
| EXIT=0 = 产品已接线 / A1 申诉 open 已通 | **否** — S1/S2 **反证**未接线；G-GAP 明示 blocked |
| G-GAP EXIT=0 = TC-E2E-027-appeal/overturn 已闭环 | **否** — GAP 表示缺席；EXIT=0 仅=诚实钉 |
| qbank `ReviewDecision` / SelectiveReview / needs_review = ManualReview | **否** — 旁证 ≠ 本 UC；S3 钉死 |
| web「人工复核」文案 = 产品已通 | **否** — 「还没开放」= honesty |
| status-machine 有 ManualReview 节 = 已实现 | **否** — TARGET / 无运行时表/API/UI |
| isolated/pgvector 绿 = sole-stack / HA / releaseEvidence | **否** — R5 mark-red；receipt `releaseEvidence=false` |
| honesty spot pass → 可放水写 covered/partial | **否** — spot 亦钉 gap/blocked；本审独立复跑一致，**仍 gap/blocked** |
| cite 对部分 UC 接受 partial 分支 → 027 可偷升 | **否** — cite 硬钉 `matrix: UC-E2E-027 remains gap/blocked`；本次未发生偷升 |

**gap/blocked 是否诚实**：**是**。S1–S5 可执行证明「无模块/HTTP/表/e2e + TARGET vs unwired + 旁证边界」+ G-GAP×6 明示 APPEAL-OPEN/OVERTURN-CAS/IDEMPOTENT/LEASE-EFFECT0/D3-STATUS/E2E；矩阵行与 P1-11 均为 **gap/blocked**；文档反复钉 ≠ covered / 禁假 partial-closed / 无 fake API；**未**把旁证或 prove 绿写成业务申诉闭环。

## 阻塞栏（升 partial / covered / 全链路前必填）

| 阻塞项 | 现状 | 关闭条件 |
|--------|------|----------|
| D3 · ManualReview schema + status-machine 运行时载重 | `GAP-UC027-D3-STATUS`；TARGET only；表=0 | 可迁移 schema + 运行时载重落地后复评；**非**仅设计文档 |
| A1 · C 端申诉 HTTP → ManualReview open | `GAP-UC027-APPEAL-OPEN`；无路由/模块 | appeal HTTP（subject+purpose+principal+幂等键+证据 snapshot）+ isolation prove；才可讨论 **partial** |
| 审核员领取/冻结（open→claimed→evidence_frozen；RLS 越权=0） | 产品缺席 | capability + CAS lease + RLS prove 后复评 |
| 单审/双审 → append-only ReviewDecision（upheld/overturned/inconclusive） | 产品缺席 | decision 路径 + prove 后复评 |
| A2 · ReviewEffect + AssessmentVersion CAS（恰一次新版本） | `GAP-UC027-OVERTURN-CAS` | overturn CAS + 审计原因 + prove；禁止覆盖原评分 |
| A3 · 同对象幂等键（重复申诉仅一条 open） | `GAP-UC027-IDEMPOTENT` | 幂等键产品 + prove 后复评 |
| A4 · 过期 lease → decision/effect=0；snapshot/consent → voided | `GAP-UC027-LEASE-EFFECT0` | lease/void 产品 + prove 后复评 |
| e2e HTTP 全套 TC-E2E-027-appeal / overturn | `GAP-UC027-E2E`；S4 无场景 | isolated HTTP：appeal→open→overturn CAS；不得借 qbank/needs_review 冒充 |
| 产品面若已浮出路由/表/模块/e2e | 当前未浮出（prove 拒浮出仍 EXIT=0） | **不得**继续用本 blocked EXIT=0 叙事；须换 TC-E2E-027-* isolation prove |
| fixture=pgvector → R5 | isolated 仍 pgvector | 按 M5/sole-stack 迁或持续 mark-red；**不因本绿宣称 HA** |
| 旁证冒充本 UC | qbank ReviewDecision / SelectiveReview / needs_review / web 文案 | 保持旁证降级；**禁止**借旁证绿宣称 027 covered/partial |
| 假 partial / covered | 矩阵/harness/eval 均 gap/blocked | **禁止**因本绿改矩阵；cite 须持续 `remains gap/blocked` |
| fake API / 假装闭环 | 本 prove 为静态 inventory | **禁止**造 appeal stub 冒充产品接线 |

**本切片不因上述阻塞而 block「诚实 gap/blocked 登记」pass**；上述 **阻塞升 partial / covered / 宣称 A1–A4 申诉闭环 / 把 EXIT=0 误写成产品已接线 / 把 mark-red 绿写成 partial-closed / 宣称全链路 E2E covered / 宣称 HA·releaseEvidence / 造 fake API**。

## 结论与建议

- **pass**；**矩阵裁定：维持 gap / blocked**（honest mark-red；产品未接线）；**禁止 covered**；**禁止**因本绿改 **partial**
- 静态诚实面成立：无 ManualReview HTTP/表/模块/e2e；TARGET 规格 vs 运行时=0；旁证边界清晰；G-GAP 六钉齐；§1b 抬 covered 路径已列
- 假绿排查通过（含「EXIT=0 冒充产品已接线/partial」对抗）；cite 钉 `UC-E2E-027 remains gap/blocked`；receipt `releaseEvidence=false`；**无 fake API**
- 与 `reviews/2026-09-10-uc-e2e-027-meetwise-honesty-spot.md` 一致方向，但本审为 **独立复跑**，不因 spot 放水
- 下一刀（升 partial 门槛）：**优先 D3 schema + A1 申诉 HTTP（ManualReview open）**；随后 overturn CAS（A2）+ 幂等/lease + e2e；在此之前矩阵 **必须 gap/blocked**
- 对照：`harness/uc-e2e-027-manual-review-appeal.md`（§1b）· `eval/uc-e2e-027-manual-review-appeal.eval.md` · `apps/api/test/uc-e2e-027-manual-review-appeal.proof.mjs` · 矩阵 `UC-E2E-027` / P1-11 · spot `reviews/2026-09-10-uc-e2e-027-meetwise-honesty-spot.md`
