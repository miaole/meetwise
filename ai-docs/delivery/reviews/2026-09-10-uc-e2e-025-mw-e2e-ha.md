# 审查归档 — UC-E2E-025 押题产物过期作面试输入 · mw-e2e-ha（+ mw-rag-route 域）

**日期**：2026-09-10（PT；本审独立复跑 ~01:57 PT）  
**审稿人**：`mw-e2e-ha`（对抗独立审；实现方不自审；覆盖 REQUEST / eval 自跑记录）  
**结论**：**pass**（诚实 gap 登记）  
**是否允许 partial**：**否**（本切片仅静态 mark-red；**矩阵必须维持 gap**；**禁止**假 partial-closed / covered）  
**releaseEvidence=false** · **Not HA** · **≠ UC-E2E-025 covered** · **本绿 ≠ 全链路 E2E covered** · **R5 green-risk** · **NON-UI** · **无 Key**

> EXIT=0 = S1–S4 库存 + G-GAP×4 诚实钉 ≠ A1/A2 产品闭环 ≠ TC-E2E-025-* 已绿。`quiz:prove` / `full.e2e` quiz / resume-derivative-reference = **旁证 ≠ covered**。

## Prove（本审独立复跑）

| CMD | EXIT | 摘要 |
|-----|------|------|
| `pnpm uc025:stale-quiz-expiry:prove` | **0** | S1–S4 + G-GAP **5 PASS**（含 G-GAP-product-surface-or-pins）；印 `GAP-UC025-STALE-REJECT` / `VERSION-PIN` / `REGEN-ENTRY` / `ACCEPT-FRESH`；`[R5-MARKED-RED]`（`E2E_PG_IMAGE=pgvector`）；receipt `.tmp/isolated-proof-receipts/2026-09-10T08-57-05-366Z-870921-f24511c1-9be2-46ab-9193-849fc743a2f0.json`（`releaseEvidence=false` · `exitCode=0` · `target=uc025:stale-quiz-expiry:prove:raw` · `outcome=passed`） |
| `pnpm eval-harness-matrix-cite:prove` | **0** | `uc-e2e-025-stale-quiz-expiry` harness+eval 引用矩阵行；钉 **gap** / ≠covered / releaseEvidence=false / Not HA / stale-quiz·version-pin / quiz:prove 旁证 / R5；**matrix: UC-E2E-025 remains gap**；`does not claim covered` |
| `pnpm quiz:prove` | 旁证（未作本 UC 验收） | 押题图本身 ≠ 过期作面试输入守卫 |

### S1–S4 + G-GAP 核对（实测 + 搜码 spot）

| ID | 声称 | 本审结果 |
|----|------|----------|
| **S1** | `interview.begin` 仅 `resume-id`；签名无 `quizId` | **PASS**（controller/service 签名 spot 一致） |
| **S2** | `0007_resume_quiz` 无产物 `expires_at` / fresh 列；0061 未加 | **PASS**（表有 `lease_expires_at` 租约列 ≠ 产物新鲜度；regex `\bexpires_at\b` 不误伤） |
| **S3** | begin 区 / contracts 无 `stale_quiz` / `quiz_expired` / `resume_version_mismatch` | **PASS** |
| **S4** | begin 路径无 `resume_quiz` 查询/join | **PASS**（begin 区 spot：无 `resume_quiz` / `quizId`） |
| **G-GAP** | 产品面未浮出 → 印齐 4 PIN；若浮出则拒 EXIT=0 | **PASS**（4 pins；≠闭环） |

## 硬钉（勾选）

- [x] **矩阵保持 gap**（行 `UC-E2E-025` + P1-8）；未升 **covered**；未假 **partial**-closed
- [x] EXIT=0 = GAP mark-red（STALE-REJECT / VERSION-PIN / REGEN / ACCEPT），**非** A1/A2 产品闭环
- [x] 钉 **R5 green-risk**；本绿 ≠ 全链路 E2E covered
- [x] `quiz:prove` / full.e2e quiz / resume-derivative-reference 标为旁证 ≠ covered
- [x] 搜码诚实：`interview.begin` 无 quizId；`resume_quiz` 无产物 expires_at；begin 不联 resume_quiz
- [x] `releaseEvidence=false` · Not HA（receipt + banner）

## 假绿排查（优先：mark-red 绿冒充 partial/covered + GAP 诚实）

| 风险说法 | 裁定 |
|---------|------|
| `uc025:stale-quiz-expiry:prove` 绿 = **covered** | **否** — 静态 inventory + GAP pin；产品 reject/accept 未接线 |
| prove 绿 = **partial**-closed / 可改矩阵 partial | **否** — harness/eval/P1-8 明示保持 **gap**；升 partial 须 HTTP：过期拒 + version pin + 重押题入口（再议）；**本审裁定：不可因 mark-red EXIT=0 改 partial** |
| G-GAP EXIT=0 = A1/A2 / TC-E2E-025-* 已闭环 | **否** — GAP 表示缺席；EXIT=0 仅=诚实钉 |
| `quiz:prove` / full.e2e quiz_ready 绿 = 025 covered | **否** — 押题生成/终态可达 ≠ 过期作面试输入守卫 |
| resume-derivative-reference 绿 = 025 | **否** — epoch/引用守卫 ≠ 产物新鲜度作面试输入 |
| isolated/pgvector 绿 = sole-stack / HA / releaseEvidence | **否** — R5 mark-red；receipt `releaseEvidence=false` |
| cite 对 025 亦接受 partial 分支 → 可偷偷改 partial | **否** — cite 软分支存在，但矩阵/harness/eval **未**改 partial；本审对抗钉死 **gap**；若文档无执行体偷升 partial → **conditional/block**（本次未发生） |

**gap 是否诚实**：**是**。S1–S4 可执行证明「产品面缺席」+ G-GAP×4 明示 STALE-REJECT/VERSION-PIN/REGEN/ACCEPT 缺口；矩阵行与 P1-8 均为 **gap**；文档反复钉 ≠ covered / 禁假 partial-closed；**未**把旁证写成业务闭环。

## 阻塞栏（升 partial / covered / 全链路前必填）

| 阻塞项 | 现状 | 关闭条件 |
|--------|------|----------|
| HTTP 过期产物开面试被拒（A1 / TC-E2E-025-stale-quiz） | `GAP-UC025-STALE-REJECT`；begin 无 quiz 输入 / 无新鲜度守卫 | e2e:isolated HTTP 注入过期产物 → 拒绝可执行并复跑绿后，才可讨论 **partial** |
| resumeVersion / 产物版本 pin（A2 / TC-E2E-025-version-mismatch） | `GAP-UC025-VERSION-PIN` | begin（或等价入口）可执行版本失配拦截后复评 |
| 失效拒绝后重押题入口 | `GAP-UC025-REGEN-ENTRY` | 拒绝路径接线重押题引导（UI/SSE/API）可证后复评 |
| 新鲜产物 + 版本 pin → 允许开面（accept 诚实） | `GAP-UC025-ACCEPT-FRESH` | reject/accept 不对称可证（不可只钉拒不钉允）后复评 |
| 产品 schema：`resume_quiz` 产物新鲜度列 / begin 接 quiz | S2/S1 库存证明缺席 | 实现落地后 **不得**继续用本 gap EXIT=0 叙事；须换 reject/accept HTTP prove |
| fixture=pgvector → R5 | isolated 仍 pgvector | 按 M5/sole-stack 迁或持续 mark-red；**不因本绿宣称 HA** |
| 旁证冒充本 UC | quiz:prove / full.e2e / derivative-ref | 保持旁证降级；**禁止**借旁证绿宣称 025 covered/partial |

**本切片不因上述阻塞而 block「诚实 gap 登记」pass**；上述 **阻塞升 partial / covered / 宣称 A1·A2 闭环 / 把 mark-red 绿写成 partial-closed / 宣称全链路 E2E covered**。

## 结论与建议

- **pass**；**矩阵裁定：维持 gap**（honest mark-red）；**禁止 covered**；**禁止**因本绿改 **partial**
- 静态诚实面成立：begin 无 quiz 输入、无 expires 新鲜度列、无 stale/version 错误码、begin 不联 resume_quiz；G-GAP 四钉齐
- 假绿排查通过；cite 钉 `UC-E2E-025 remains gap`；receipt `releaseEvidence=false`
- 下一刀（升 partial 门槛）：HTTP 过期拒绝 + resumeVersion pin + 重押题入口 + accept 新鲜路径进 isolated；在此之前矩阵 **必须 gap**
- 对照：`harness/uc-e2e-025-stale-quiz-expiry.md` · `eval/uc-e2e-025-stale-quiz-expiry.eval.md` · `apps/api/test/uc-e2e-025-stale-quiz-expiry.proof.mjs` · 矩阵 `UC-E2E-025` / P1-8 · REQUEST `reviews/REQUEST-2026-09-10-uc-e2e-025-for-mw-e2e-ha.md`
