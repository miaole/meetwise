# 审查归档 — UC-E2E-003 i18n / locale 结构面 · mw-e2e-ha

**日期**：2026-09-10（PT；本审独立复跑 ~01:38 PT）  
**审稿人**：`mw-e2e-ha`（对抗独立审；实现方不自审）  
**结论**：**pass**  
**是否允许 partial**：**是**（矩阵 **gap→partial** 可支撑；**禁止**升 **covered**）  
**releaseEvidence=false** · **Not HA** · **≠ UC-E2E-003 covered** · **本绿 ≠ 全链路 E2E covered** · **NON-UI 静态 inventory** · Playwright **secondary / 未接线** · **无 MODEL_API_KEY**

> 用户硬要求：非 UI 为主。本切片静态 prove 可支撑 **partial**；**不得**因静态绿升 covered / 冒充 DOM e2e。

## Prove（本审独立复跑）

| CMD | EXIT | 摘要 |
|-----|------|------|
| `pnpm uc003:i18n-locale:prove` | **0** | S1–S3 **PASS**；G-GAP×3 诚实钉（`GAP-UC003-ERROR-CODE-EN-MAP` / `HARDCODED-ZH-UI` / `DOM-E2E-UI`）；`[R5-MARKED-RED]` banner 已印（isolated 包装夹具；**静态体不读写 DB** ≠ 本 UC 向量假绿）；receipt `.tmp/isolated-proof-receipts/2026-09-10T08-38-07-280Z-828022-a0a325af-0a22-4088-98a1-19fffe42671f.json`（`release_evidence=false`） |
| `pnpm eval-harness-matrix-cite:prove` | **0** | `uc-e2e-003-i18n-locale` harness+eval 引用矩阵行；钉 partial / ≠covered / releaseEvidence=false / Not HA / i18n·locale / error-code en map GAP / Playwright secondary·DOM / UC003 GAP honesty；**matrix: UC-E2E-003 is partial (not covered)**；`does not claim covered` |

### S1–S3 + G-GAP 核对（实测 + 源码 spot）

| ID | 声称 | 本审结果 |
|----|------|----------|
| **S1** | next-intl 管道：`LOCALES` 含 zh+en；`messages/{en,zh}.json`；locale cookie action | **PASS**（`i18n/request.ts`：`LOCALES=['zh','en']` + `DEFAULT_LOCALE`；`locale-actions.ts` cookie `set('locale', …)`） |
| **S2** | en/zh flatten key **parity**（无 orphan） | **PASS**（本审 spot：enKeys=**144** / zhKeys=**144**；onlyEn=0 / onlyZh=0） |
| **S3** | `en.json` 无意外 CJK；allowlist=`home.footBrand` / `common.zh` | **PASS**（unexpectedCJK=0；allowlist 两键均含故意 CJK） |
| **G-GAP-1** | `ERROR_MESSAGES` zh-only + `mapResumeUploadError` 无 locale → `GAP-UC003-ERROR-CODE-EN-MAP` | **PASS（诚实钉）**：prove 印 `zh-only (20/20 codes)`；源码 spot 确认全中文文案、`mapResumeUploadError(status, body)` **无 locale 参** |
| **G-GAP-2** | hot lib 硬编码 zh UI → `GAP-UC003-HARDCODED-ZH-UI` | **PASS（诚实钉）**：prove 印 **50** CJK string-literal lines；spot `view-model.ts` 等确有中文 UI 字面量 |
| **G-GAP-3** | `e2e-ui/*` 无 locale/en DOM 断言 → `GAP-UC003-DOM-E2E-UI` | **PASS（诚实钉）**：specs=`golden/online-public/recruiting-bound/screenshots/stream-window/voice-duplex`；`rg` **NO_LOCALE_HITS** |

## 硬钉（勾选）

- [x] **≠ covered**（prove/cite 绿 ≠ UC-E2E-003 covered；矩阵行 **partial**；P1-6 **禁止**升 covered）
- [x] **releaseEvidence=false** · Not HA
- [x] 未把 en/zh **键对等** / en.json CJK allowlist 冒充 **DOM 无中文残留** / 产品 i18n **covered**
- [x] 未把 G-GAP-* EXIT=0 写成 A1/A2 已闭环 / 错误码 en 映射已闭环
- [x] 未把 `prove:public-copy` 冒充本 UC 验收
- [x] Playwright 标 **secondary** / 未接线（NON-UI 优先合规）
- [x] R5 banner 出现但静态 inventory **不读写 DB** — harness 已钉「≠本 UC 向量假绿」；**不得**借 R5 否认本绿，也**不得**借本绿宣称 sole-stack/HA

## 假绿排查（优先：静态键对等冒充 DOM / 产品 i18n covered）

| 风险说法 | 裁定 |
|---------|------|
| `uc003:i18n-locale:prove` 绿 = **UC-E2E-003 covered** | **否** — 最多 **partial**；G-GAP×3 明示 A2/硬编码 zh/DOM e2e-ui 未闭环 |
| en/zh key parity 绿 = **DOM 无中文残留** / locale=en UI 已扫 | **否** — S2/S3 仅 `messages/*.json`；G-GAP-2 已钉 hot lib 50 行硬编码 zh；G-GAP-3 钉无 DOM e2e-ui |
| `ERROR_MESSAGES` 库存绿 / GAP pin EXIT=0 = A2 错误码→en 已闭环 | **否** — 20/20 zh-only + 无 locale 分支 = **诚实缺口**，≠产品接线 |
| `public-copy` 绿 = 003 covered | **否** — 营销禁语旁证；harness §0/§3 假绿标红已钉 |
| e2e-ui 随便绿 / screenshots 绿 = 003 locale DOM | **否** — 无专用 locale/en DOM 断言（G-GAP-3） |
| isolated/R5 banner = 本 UC 向量假绿或 sole-stack/HA | **否** — 静态体不碰 DB；receipt `release_evidence=false`；≠ HA |
| cite EXIT=0 = 业务 covered | **否** — cite 仅 harness/eval/矩阵引用与 partial 钉；脚本 NOTE 已钉 ≠业务 covered |

**partial 是否诚实**：**是**。可执行 S1–S3 结构合同（next-intl 管道 + 144 键对等 + en CJK allowlist）+ 三条 G-GAP 诚实 mark-red 已绿，足以支撑矩阵 **gap→partial** + P1-6「静态 prove 已挂」；缺口仍明示，**未**把静态资源绿写成 DOM/产品 i18n covered，**未**假升 covered。

## 阻塞栏（升 covered / 全链路前必填）

| 阻塞项 | 现状 | 关闭条件 |
|--------|------|----------|
| A2 错误码→locale/en 映射产品闭环 | `ocr-preview-ui.ts` `ERROR_MESSAGES` **20/20 zh-only**；`mapResumeUploadError` **无 locale 参**；`GAP-UC003-ERROR-CODE-EN-MAP` | 产品 en map / 迁入 `messages` + 可执行断言（含校验错误 en 文案）后复评 |
| A1 硬编码 zh UI（hot lib → 全仓） | G-GAP-2 钉 view-model / ocr-preview-ui / interview-state **≥50** CJK 字面量行；全仓清扫未做 | 热路径文案迁入 i18n；locale=en 下无残留中文 UI（至少 hot lib）后复评 |
| Playwright `locale=en` DOM e2e-ui | `GAP-UC003-DOM-E2E-UI`；`e2e-ui/*` **无** locale/en DOM 断言；**未**进 `e2e:ui:isolated` | 显式 locale=en DOM 扫描 + 错误码 en 文案进 isolated UI 复跑绿后复评（仍 secondary 于产品映射闭环） |
| 模型产出语言跟随 locale | harness **明确不测**（归 ai-eval / e2e-scenarios D6） | ai-eval 另轨；**禁止** fake-model / 本静态绿冒充 |
| `public-copy` 旁证冒充 | 存在但 ≠ 本 UC | 保持另轨；**禁止**并入 covered 论据 |
| fixture/R5（若仍经 isolated） | banner 可能印 pgvector；本 prove 静态不读写 DB | 持续 mark-red 即可；**不因本绿宣称 HA / sole-stack / releaseEvidence** |

**本切片不因上述阻塞而 block partial**；上述仅 **阻塞升 covered / 宣称全链路 E2E covered / 把静态键对等或 GAP pin EXIT=0 写成 DOM/产品 i18n 闭环**。

## 结论与建议

- **pass**；**允许 gap→partial**（维持矩阵 **partial** + P1-6）；**禁止 covered**
- 静态结构面诚实：S1–S3 绿 + 3× GAP pin；**≠** DOM e2e / 错误码 en 映射闭环
- 假绿排查通过：键对等未冒充 DOM；GAP 为诚实钉；public-copy 已降级；Playwright secondary
- 下一刀产品债（P1）：`ERROR_MESSAGES` en map + hot-lib 硬编码 zh 迁入 i18n；并行（降次）e2e-ui locale=en DOM
- 对照：`harness/uc-e2e-003-i18n-locale.md` · `eval/uc-e2e-003-i18n-locale.eval.md` · `apps/web/test/uc-e2e-003-i18n-locale.proof.mjs` · 矩阵 `UC-E2E-003` / §3 **P1-6**
