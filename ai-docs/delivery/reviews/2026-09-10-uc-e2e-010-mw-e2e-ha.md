# 审查归档 — UC-E2E-010 SSE 断线→Last-Event-ID 续传 · mw-e2e-ha

**日期**：2026-09-10（PT；本审独立复跑 ~01:34 PT）  
**审稿人**：`mw-e2e-ha`（对抗独立审；实现方不自审）  
**结论**：**pass**  
**是否允许 partial**：**是**（矩阵维持 **partial** + P1-1；**禁止**升 **covered**）  
**releaseEvidence=false** · **Not HA** · **≠ UC-E2E-010 covered** · **本绿 ≠ 全链路 E2E covered** · **R5 green-risk** · **NON-UI** · **无 Key**

> privacy spot 对本 UC 的 R-authz/stub 旁注 **pass** **不改变**本审门槛：不得因 spot 放水升 covered / 宣称隐私擦除或全链路 E2E covered。

## Prove（本审独立复跑）

| CMD | EXIT | 摘要 |
|-----|------|------|
| `pnpm uc010:sse-resume:prove` | **0** | R1–R4 + R-authz + G-GAP **9 PASS**；`[R5-MARKED-RED]`（`E2E_PG_IMAGE=pgvector`）；receipt `.tmp/isolated-proof-receipts/2026-09-10T08-34-41-442Z-818129-03c936d2-d92c-4687-9d23-65d2d12eac31.json`（`releaseEvidence=false` · `exitCode=0` · `target=uc010:sse-resume:prove:raw` · `outcome=passed`）；印 `GAP-UC010-PRIVACY-STUB` + FULL-E2E / A3-KILL-BILLING / UI / LAYER |
| `pnpm eval-harness-matrix-cite:prove` | **0** | `uc-e2e-010-sse-resume` harness+eval 引用矩阵行；钉 partial / ≠covered / releaseEvidence=false / Not HA / Last-Event-ID / disconnect-resume / GAP honesty / layer cites；**matrix: UC-E2E-010 is partial (not covered)**；`does not claim covered` |

### R1–R4 + R-authz + G-GAP 核对（实测）

| ID | 声称 | 本审结果 |
|----|------|----------|
| **R1** | 全量 catch-up 后 abort；seq=[1,2,3]；kinds=question_ready/waiting_user/progress | **PASS**（3 条） |
| **R2** | 断线窗口账本续写 → `Last-Event-ID=3` → 仅 seq=[4,5]；无 seq≤3 重复 | **PASS**（2 条） |
| **R3** | `Last-Event-ID=2` → seq=[3,4,5] | **PASS** |
| **R4** | `Last-Event-ID=5` → SSE 200 + 空 ids + hold `: ping`（非错误空） | **PASS** |
| **R-authz** | userB 越权订阅 → **404** | **PASS**（≠隐私擦除 / 0058 fence covered） |
| **G-GAP** | 打印 GAP-UC010-*（FULL-E2E / A3 / UI / LAYER）+ PRIVACY-STUB；EXIT=0 仅=诚实钉 | **PASS**（印齐；≠闭环） |

## 硬钉（勾选）

- [x] **≠ covered**（prove 绿 ≠ UC-E2E-010 covered；cite 亦钉；矩阵行 **partial**）
- [x] **R5 green-risk**（isolated → pgvector；banner 已印；≠ sole-stack / HA）
- [x] **releaseEvidence=false** · Not HA
- [x] 未把 `helpers/sse` / `last-event-id:unit:prove` / `sse-slot:prove` / `stream-window.spec.ts` / `api:validate` 冒充本 UC 业务 covered
- [x] 未把 G-GAP EXIT=0 写成 A3 kill/无双扣 / full.e2e 已闭环
- [x] 未因 privacy spot pass 放水（R-authz=404 + PRIVACY-STUB ≠ 0058 / 擦除 covered）

## 假绿排查（优先：层旁证冒充 + GAP 诚实）

| 风险说法 | 裁定 |
|---------|------|
| `uc010:sse-resume:prove` 绿 = **covered** | **否** — 最多 **partial**；缺 full.e2e mid-interview 断线 + A3 kill/无双扣 + UI 重连 |
| `last-event-id:unit` / `sse-slot` / `helpers/sse` 绿 = 010 covered | **否** — 层旁证；harness/eval/cite/G-GAP-LAYER 均钉 ≠ 本 UC 业务 |
| `stream-window` 绿 = 断线续传 covered | **否** — UI 10k 窗口压力 ≠ Last-Event-ID 断线续传 |
| G-GAP / PRIVACY-STUB EXIT=0 = A3 / 0058 / full.e2e 闭环 | **否** — 诚实钉；prove 明确「EXIT=0≠covered」 |
| `api:validate` 单事件空 catch-up = 多 seq 断线续写后重连 | **否** — 缺断线窗口续写后仅 seq>N |
| isolated/pgvector 绿 = sole-stack / HA / releaseEvidence | **否** — R5 mark-red；receipt `releaseEvidence=false` |
| privacy spot pass → 可升 covered / 隐私擦除 covered | **否** — spot 不抬门槛；本 prove 仅 minimal stub + 404 |

**partial 是否诚实**：**是**。HTTP 集成 R1–R4（abort→seed→`Last-Event-ID=N` 仅 seq>N；跨用户 404）+ G-GAP 诚实钉可执行合同已绿，足以支撑矩阵 **partial** + P1-1「HTTP prove 已挂」；缺口仍明示，**未**把 unit/slot/helpers/stream-window 写成业务 E2E covered，**未**假升 covered。

## 阻塞栏（升 covered / 全链路前必填）

| 阻塞项 | 现状 | 关闭条件 |
|--------|------|----------|
| `full.e2e` / `e2e:isolated` mid-interview SSE kill→resume | G-GAP `GAP-UC010-FULL-E2E` 诚实钉；未接线；无 Key 时 isolated 另 blocked | 真面试链路中途断 SSE → `Last-Event-ID` 续传进 isolated 复跑绿后复评 |
| A3 进程 kill + 无双扣费 / seq 无洞 | `GAP-UC010-A3-KILL-BILLING`；本 prove 不碰 worker+commerce/额度 | worker+commerce 路径可执行断言（无双扣、seq 连续）后复评 |
| Playwright UI 断线重连 | `GAP-UC010-UI`；`stream-window` ≠ 断线续传 | UI E2E 显式断线→续传（降次于 HTTP）接线后复评 |
| 完整 0058 privacy fence | `_neg-harness` 未载 0058；本 prove **minimal stub**（`GAP-UC010-PRIVACY-STUB`）；R-authz 仅 404 | 载入真实 privacy fence + 擦除/越权合同；**禁止**借 stub/spot 宣称隐私 covered |
| 跨副本 SSE 槽 | harness 钉 `HC-GAP-008`；`sse-slot:prove` 仅进程内 | 跨副本槽位合同可执行后另评（≠本 UC covered 捷径） |
| fixture=pgvector → R5 | isolated 仍 pgvector | 按 M5/sole-stack 迁关系 prove→MySQL 或持续 mark-red；**不因本绿宣称 HA** |

**本切片不因上述阻塞而 block partial**；上述仅 **阻塞升 covered / 宣称全链路 E2E covered / 把层旁证或 privacy stub 写成业务闭环**。

## 结论与建议

- **pass**；**允许并维持**矩阵 **partial**（P1-1）；**禁止 covered**
- HTTP 合同诚实：abort→seed→`Last-Event-ID=N` 仅 seq>N；跨用户 404；GAP pins 齐
- 层旁证 cite 正确降级；假绿排查通过；**不因 privacy spot 放水**
- 下一刀 P1：`full.e2e`/`e2e:isolated` mid-interview 断线续传；并行 A3 kill/无双扣；privacy 去 stub
- 对照：`harness/uc-e2e-010-sse-resume.md` · `eval/uc-e2e-010-sse-resume.eval.md` · `apps/api/test/uc-e2e-010-sse-resume.proof.ts` · 矩阵 `UC-E2E-010` / P1-1
