# 审查归档 — UC-E2E-018 用户放弃面试 · mw-e2e-ha

**日期**：2026-09-10（PT；本审复跑 ~01:10–01:11 PT）  
**审稿人**：`mw-e2e-ha`（对抗独立审；实现方不自审）  
**双域**：`mw-privacy-int` spot 已对配额/预留旁证结论 **pass**（见 `reviews/2026-09-10-uc-e2e-018-mw-privacy-int-spot.md`）。**本审不因 spot pass 放水**；E2E/矩阵阶梯独立复跑 prove + 假绿排查后另下结论。  
**结论**：**pass**  
**是否允许 partial**：**是**（矩阵维持 **partial** + P0-8；**禁止**升 **covered**）  
**releaseEvidence=false** · **Not HA** · **≠ UC-E2E-018 covered** · **本绿 ≠ 全链路 E2E covered** · **R5 green-risk** · **NON-UI 为主**

## Prove（本审独立复跑）

| CMD | EXIT | 摘要 |
|-----|------|------|
| `pnpm uc018:abandon:prove` | **0** | A1 / A1-shell / A2 / A3 / A-created-reserved **共 25 PASS**；R5-MARKED-RED（`E2E_PG_IMAGE=pgvector`）；receipt `.tmp/isolated-proof-receipts/2026-09-10T08-10-52-846Z-740068-4b96744b-37fe-45a9-9a2a-8a5d00eff73b.json`；`releaseEvidence=false`；打印 `BLOCKED_FOR_FULL_E2E` |
| `pnpm eval-harness-matrix-cite:prove` | **0** | `uc-e2e-018-user-abandon` harness+eval 引用矩阵行；钉 partial / ≠covered / releaseEvidence=false / Not HA；**matrix: UC-E2E-018 is partial (not covered)** |

### A1–A3 核对（实测）

| ID | 声称 | 本审结果 |
|----|------|----------|
| **A1** | active+reserved → abandoned + released + 额度净变 0 | **PASS**（reserve→reserved；5→4→5；interview/consumption 终态） |
| **A1-shell** | created 无预留 → abandoned + noop | **PASS** |
| **A-created-reserved** | created+reserved → abandoned + released；净变 0 | **PASS** |
| **A2** | 二次 → already_abandoned+noop；complete 拒复活 | **PASS**（`interview_settlement_failed` / already_released 路径；仍 abandoned） |
| **A3** | abandoned ∉ in-progress（`NOT IN (completed,abandoned,failed)`） | **PASS** |

## 硬钉（勾选）

- [x] **≠ covered**（prove 绿 ≠ UC-E2E-018 covered；cite 亦钉；矩阵行 **partial**）
- [x] **R5 green-risk**（isolated → pgvector；banner 已印；≠ sole-stack / HA）
- [x] **releaseEvidence=false** · Not HA
- [x] 未把 `commerce:prove` / `commerce-reconcile:prove` 冒充本 UC 专用验收
- [x] 未把 API 已有 `POST …/abandon` 或 `neg:interview`/`api:validate` 旁证写成 **full.e2e / e2e:isolated 已含放弃口**
- [x] 双域：隐私 spot 配额旁证 ≠ 本审 E2E 阶梯放行；禁止与隐私 DELETE/擦除闭环混读

## 假绿排查

| 风险说法 | 裁定 |
|---------|------|
| `uc018:abandon:prove` 绿 = **covered** | **否** — 最多 **partial**；缺 full.e2e HTTP 放弃场景与图终态 |
| `commerce:prove` / reconcile TTL 绿 = 018 关闭 | **否** — 另轨旁证 ≠ A1–A3 专用合同 |
| API 已有 `POST /interview/:id/abandon`（controller+service）= E2E 口已关 | **否** — `e2e/full.e2e.ts` **无** abandon 引用；`neg:interview` / `api:validate` 负/校验旁证 ≠ UC-E2E-018 full.e2e 验收体 |
| isolated/pgvector 绿 = sole-stack / HA / releaseEvidence | **否** — R5 mark-red；receipt `releaseEvidence=false` |
| 额度回补 = 隐私删除闭环 | **否** — 双域隔离；删除仍须 DELETE=503 另轨 |
| 空断言 / stub harness | **否** — 25 条真 DB 断言；cite 钉 harness 非 stub；矩阵非 covered |

**partial 是否诚实**：**是**。db 集成 A1–A3（含 shell / created-reserved）可执行合同已绿，足以支撑矩阵 **partial** + P0-8「集成 prove 已挂」；缺口仍明示，**未**假升 covered。

## 阻塞栏（升 covered / 全链路前必填）

| 阻塞项 | 现状 | 关闭条件 |
|--------|------|----------|
| HTTP 放弃口进 `full.e2e` / `e2e:isolated` | API 口存在（`interview.controller` `@Post(':id/abandon')`）；**未**进 `e2e/full.e2e.ts` 场景矩阵 | 显式 TC：点放弃/HTTP abandon → abandoned + released + 不可 resume；复跑绿后才可讨论更高覆盖 |
| `AiGraphRun → safely_terminated` | `abandonInterviewAndRelease` **不碰** graph run；需求主流程第 3 步未钉 | 集成/E2E 断言 safe_terminating→safely_terminated + 业务事实保全 |
| TTL sweeper → abandoned | 属 `commerce-reconcile` 另轨；≠ 本 prove | 专用钉或 UC 旁证合同显式接线后复评 |
| `waiting_user` interview 放弃 | abandon CAS 仅 `created\|active`（`commerce.ts`）；`failInterview` 含 `waiting_user`，abandon **不含**；与场景「active/waiting_user → abandoned」不完全对齐 | 扩展 CAS 或产品确认 interview 行永不落 `waiting_user` 后补 prove |
| UI「点放弃」 | NON-UI 切片；Playwright 未要求本审 | 产品接线后 UI E2E（降次于 HTTP） |

**本切片不因上述阻塞而 block partial**；上述仅 **阻塞升 covered / 宣称全链路 E2E covered**。

## 结论与建议

- **pass**；**允许并维持**矩阵 **partial**（P0-8）；**禁止 covered**
- 双域：`mw-e2e-ha` = pass（阶梯/假绿）；`mw-privacy-int` spot = pass（配额旁证）——互不替代
- 下一刀 P0：HTTP abandon 场景进 `full.e2e` / `e2e:isolated`；并行评估 `waiting_user` CAS 与 AiGraphRun 终态钉
- 对照：`harness/uc-e2e-018-user-abandon.md` · `eval/uc-e2e-018-user-abandon.eval.md` · `packages/db/test/uc-e2e-018-user-abandon.proof.ts` · 矩阵 `UC-E2E-018` / P0-8 · spot `reviews/2026-09-10-uc-e2e-018-mw-privacy-int-spot.md`
