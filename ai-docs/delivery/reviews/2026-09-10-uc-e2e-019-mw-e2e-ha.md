# 审查归档 — UC-E2E-019 报告重生成 + 与退款并发 · mw-e2e-ha

**日期**：2026-09-10（PT；本审复跑 ~01:25–01:26 PT）  
**审稿人**：`mw-e2e-ha`（对抗独立审；实现方不自审）  
**双域**：`mw-model-op` spot 已对 D1/账本边界结论 **pass**（见 `reviews/2026-09-10-uc-e2e-019-mw-model-op-spot.md`）。**本审不因 spot pass 放水**；E2E/矩阵阶梯独立复跑 prove + 假绿排查后另下结论。  
**结论**：**pass**  
**是否允许 partial**：**是**（矩阵维持 **partial** + P1-5；**禁止**升 **covered**）  
**releaseEvidence=false** · **Not HA** · **≠ UC-E2E-019 covered** · **本绿 ≠ 全链路 E2E covered** · **R5 green-risk** · **NON-UI** · **无 Key**

## Prove（本审独立复跑）

| CMD | EXIT | 摘要 |
|-----|------|------|
| `pnpm uc019:report-regenerate:prove` | **0** | G1–G4 **全 PASS**；R5-MARKED-RED（`E2E_PG_IMAGE=pgvector`）；receipt `.tmp/isolated-proof-receipts/2026-09-10T08-25-53-250Z-791603-3d827d6d-c2b4-4900-a68f-f28a05f8f244.json`（`releaseEvidence=false` · `exitCode=0` · `target=uc019:report-regenerate:prove:raw`）；打印 `GAP-UC019-REFUND-FIRST-ORDER` / `GAP-UC019-QUARANTINE-REGEN` / `GAP-UC019-REGEN-IDEM-KEY` / `GAP-UC019-HTTP-REGENERATE` + `BLOCKED_FOR_FULL_E2E` |
| `pnpm eval-harness-matrix-cite:prove` | **0** | `uc-e2e-019-report-regenerate` harness+eval 引用矩阵行；钉 partial / ≠covered / releaseEvidence=false / Not HA / regenerate·requeue / concurrency honesty / UC019 GAP；**matrix: UC-E2E-019 is partial (not covered)** |

### G1–G4 核对（实测）

| ID | 声称 | 本审结果 |
|----|------|----------|
| **G1** | failed→`requeueFailedReport` 连点：首次 true→queued；二/三次 false；额度不扣；consumption 仍 confirmed | **PASS**（pre failed+confirmed+额度4；rq1 true→queued；rq2/rq3 false；post queued；额度仍4；confirmed） |
| **G2** | `requeueFailedReport` ∥ `releaseConsumption`：release→`already_confirmed`；无 `released∧(queued\|running)`；额度不退 | **PASS**（rel=`already_confirmed`；cons=confirmed；units=4；report queued；非法组合否；requeue 赢→queued∧confirmed） |
| **G3** | A3 退款先赢路径 **GAP** + quarantined 不可 requeue（UC-011 R2 **BLOCKED**） | **PASS**（诚实钉：confirmed 上 release→`already_confirmed`，无法先到 released；poison→quarantined；`requeueFailedReport=false`；印 `GAP-UC019-REFUND-FIRST-ORDER` / `GAP-UC019-QUARANTINE-REGEN`） |
| **G4** | `ai_report` 无 regenerate* 列；印 GAP；EXIT=0 仅=诚实钉 ≠ covered | **PASS**（无 regenerate* 列；有 status/attempts；印 `GAP-UC019-REGEN-IDEM-KEY` / `GAP-UC019-HTTP-REGENERATE`；明示 ≠ covered） |

## 硬钉（勾选）

- [x] **≠ covered**（prove 绿 ≠ UC-E2E-019 covered；cite 亦钉；矩阵行 **partial**）
- [x] **R5 green-risk**（isolated → pgvector；banner 已印；≠ sole-stack / HA）
- [x] **releaseEvidence=false** · Not HA
- [x] 未把 `uc011:report-refund:prove` / `report:prove` / `commerce:prove` 冒充本 UC 专用验收
- [x] **未把 `requeueFailedReport` 代理写成产品 regenerate 闭环 covered**（见下）
- [x] 未把 G3/G4 EXIT=0 写成 A3 / HTTP regenerate 已闭环
- [x] 双域：model-op spot ≠ 本审 E2E 阶梯放行；**禁因 spot 升 covered**

## 假绿排查（优先：requeue ≠ regenerate 闭环）

| 风险说法 | 裁定 |
|---------|------|
| `uc019:report-regenerate:prove` 绿 = **covered** | **否** — 最多 **partial**；缺 HTTP regenerate / regenerateAttempt / A3 refund-first 闭环 / quarantine regen 出口 |
| G1 `requeueFailedReport` 绿 = 产品 regenerate（含幂等键 `(interviewId, regenerateAttempt)`）**已闭环 covered** | **否** — `report.ts` `requeueFailedReport` 仅为 `status='failed'→queued` CAS；**无** regenerateAttempt 列/API；G4 诚实钉「无 regenerate* 列」；harness/eval 明示「requeue 作 regenerate **代理**」 |
| G3/G4 EXIT=0 = A3「退款先到→released→regen 拒」/ HTTP regenerate **已闭环** | **否** — G3/G4 是 **honesty GAP pin**：断言无法 confirmed→released、quarantine 不可 requeue、无 regenerate* 列，并打印 GAP；明确「EXIT=0 仅=诚实钉，≠ covered」 |
| `uc011` R2 绿 = 019 covered | **否** — R2 仅钉 quarantine 不可 requeue；≠ regenerate×退款并发 G1–G2 |
| `report:prove` requeue / bulkhead 绿 = 019 covered | **否** — 另轨舱壁 ≠ UC-E2E-019 专用合同 |
| model-op spot pass = 可升 covered / 放水阶梯 | **否** — spot 仅 D1/账本旁证；本审独立复跑；互不替代 |
| apps / `full.e2e` 已有 regenerate HTTP = E2E 口已关 | **否** — apps/e2e **无** `POST /reports/:id/regenerate`；G4 钉 `GAP-UC019-HTTP-REGENERATE` |
| isolated/pgvector 绿 = sole-stack / HA / releaseEvidence | **否** — R5 mark-red；receipt `releaseEvidence=false` |
| 空断言 / stub harness / 矩阵假 covered | **否** — G1–G4 真 DB 断言；cite 钉 harness 非 stub、**does not claim covered**；矩阵 **partial** |

**partial 是否诚实**：**是**。db 集成 G1–G2（requeue 幂等 + requeue∥release 无非法组合）+ G3/G4 诚实 GAP pin 可执行合同已绿，足以支撑矩阵 **partial** + P1-5「集成 prove 已挂」；缺口仍明示，**未**把 requeue 代理写成 regenerate 产品闭环 covered，**未**假升 covered。

## 阻塞栏（升 covered / 全链路前必填）

| 阻塞项 | 现状 | 关闭条件 |
|--------|------|----------|
| HTTP `POST /reports/:id/regenerate` 进 `full.e2e` / `e2e:isolated` | apps/e2e **无** regenerate 路由；G4 钉 `GAP-UC019-HTTP-REGENERATE` | 产品 HTTP 口 + 显式 TC（连点幂等 + 额度不扣）进 isolated 复跑绿 |
| 幂等键 `(interviewId, regenerateAttempt)` | `ai_report` **无** regenerate* 列；仅 `requeueFailedReport` failed→queued CAS 代理 | schema/API 落 regenerateAttempt（或等价）+ TC-E2E-019-regen-idem 产品口断言 |
| A3 退款先赢→released→regen 拒 | `releaseConsumption` 对 confirmed→`already_confirmed`；**无** confirmed→released 产品口；G3 钉 `GAP-UC019-REFUND-FIRST-ORDER` | 产品路径（或显式状态守卫）可执行后：退款先到→released→regen 拒且可解释 |
| quarantined 免费 regenerate 出口 | `requeueFailedReport` 对 quarantined=false（UC-011 R2）；G3 钉 `GAP-UC019-QUARANTINE-REGEN` | 产品 quarantine→regen 出口接线 + 与额度守卫合同后再评；**禁止**借 requeue CAS 冒充 |
| balance-ui / 支付退款回调 | 另轨 UC-011 R4；本 NON-UI 切片未要求 | 011/019 产品口并联后 UI/回调 E2E（降次于 HTTP） |
| fixture=pgvector → R5 | isolated 仍 pgvector | 按 M5/sole-stack 迁关系 prove→MySQL 或持续 mark-red；**不因本绿宣称 HA** |

**本切片不因上述阻塞而 block partial**；上述仅 **阻塞升 covered / 宣称全链路 E2E covered / 把 requeue 代理写成 regenerate 闭环 covered**。

## 结论与建议

- **pass**；**允许并维持**矩阵 **partial**（P1-5）；**禁止 covered**
- 双域：`mw-e2e-ha` = pass（阶梯/假绿）；`mw-model-op` spot = pass（D1/账本）——互不替代；**禁因 spot 升 covered**
- D1 边界未被写反：报告失败默认不退（G1/G2 confirmed 不变）；regenerate（代理）免费不扣费；confirmed 拒误 release（G2）
- G3/G4 诚实：A3 退款先赢 / quarantine regen / regenerateAttempt / HTTP = GAP pin，**≠** 产品闭环 covered（假绿排查通过）
- 下一刀 P1：HTTP regenerate + regenerateAttempt 幂等键；并行评估 A3 confirmed→released→regen 拒与 quarantine regen 出口
- 对照：`harness/uc-e2e-019-report-regenerate.md` · `eval/uc-e2e-019-report-regenerate.eval.md` · `packages/db/test/uc-e2e-019-report-regenerate.proof.ts` · 矩阵 `UC-E2E-019` / P1-5 · spot `reviews/2026-09-10-uc-e2e-019-mw-model-op-spot.md`
