# 审查归档 — UC-E2E-028 trace/账本失败不阻塞 · mw-e2e-ha（对抗独立审）

**日期**：2026-09-10（PT；本审独立复跑 ~02:04–02:05 PT）  
**审稿人**：`mw-e2e-ha`（对抗独立审；实现方不自审；**不**因 `mw-model-op` spot pass 放水）  
**结论**：**pass**（诚实 gap 登记）  
**是否允许 partial**：**否**（本切片仅静态 inventory + GAP mark-red；**矩阵必须维持 gap**；**禁止**假 partial-closed / covered）  
**releaseEvidence=false** · **Not HA** · **≠ UC-E2E-028 covered** · **本绿 ≠ 全链路 E2E covered** · **R5 green-risk** · **NON-UI** · **无 Key**

> EXIT=0 = S1–S5 库存 + G-GAP×4 诚实钉 ≠ A1 fail-open / A2 recon / A3 E2E 闭环 ≠ TC-E2E-028-* 已绿。  
> **核心对抗钉**：`persistTrace` 与 `settleAiTextCost` / `completeModelInvocation` **同 `asPrincipal` 事务耦合** → INSERT 失败落入 catch → `external_outcome_unknown`（**fail-closed 成功路径**），**≠** 需求 A1 fail-open。  
> `report-bulkhead` / `releaseSharedAdmissionBestEffort` / `reqid` / usage-calibration-reconciler = **旁证 ≠ covered**。

## Prove（本审独立复跑）

| CMD | EXIT | 摘要 |
|-----|------|------|
| `pnpm uc028:trace-fail-open:prove` | **0** | S1–S5 + G-GAP **6 PASS**（含 G-GAP-product-surface-or-pins）；印 `GAP-UC028-FAIL-OPEN` / `RECON` / `TRUTH-BLOCK-E2E` / `INJECT`（GAPS=4）；`[R5-MARKED-RED]`（`E2E_PG_IMAGE=pgvector`）；receipt `.tmp/isolated-proof-receipts/2026-09-10T09-04-53-828Z-892519-c1e92fdc-7f29-4181-8d55-cccdaed53e2c.json`（`releaseEvidence=false` · `exitCode=0` · `target=uc028:trace-fail-open:prove:raw` · `outcome=passed`） |
| `pnpm eval-harness-matrix-cite:prove` | **0** | `uc-e2e-028-trace-ledger-fail-open` harness+eval 引用矩阵行；钉 **gap** / ≠covered / releaseEvidence=false / Not HA / trace·fail-open / recon / report-bulkhead 旁证 / R5；**matrix: UC-E2E-028 remains gap**；`does not claim covered` |
| report-bulkhead / `reqid:prove` / admission best-effort | 旁证（未作本 UC 验收） | 报告舱壁 / 并发槽释放 ≠ `ai_invocation_trace` 写失败不阻塞 |

### S1–S5 + G-GAP 核对（实测 + 搜码 spot）

| ID | 声称 | 本审结果 |
|----|------|----------|
| **S1** | `persistTrace` + INSERT `ai_invocation_trace`；注释「无条件写入」 | **PASS**（`invoke.ts` spot 一致） |
| **S2** | 与 settle/complete **同事务**；catch → `external_outcome_unknown`；无 `.catch` fail-open 包装 | **PASS**（见下对抗）；**反证 fail-open 未接线** |
| **S3** | 无 missing-trace rewrite/recon；usage-calibration ≠ UC-028 | **PASS** |
| **S4** | `e2e/*.e2e.ts` / full.e2e **无** UC-E2E-028 / TC-E2E-028 | **PASS** |
| **S5** | spec best-effort vs code 耦合；report-bulkhead 旁证在且不宣称 UC-028 | **PASS**（spec≠code 隔离已钉） |
| **G-GAP** | 产品面未浮出 → 印齐 4 PIN；若旁路/队列/e2e 浮出则拒 EXIT=0 | **PASS**（4 pins；≠闭环） |

**搜码对抗（同事务耦合 · 禁止误读为 fail-open）**  
`packages/ai-runtime/src/invoke.ts` settle 区：`await asPrincipal(...) { settleAiTextCost → completeModelInvocation → if (!error) await persistTrace(...) }`；同 try 的 catch 返回 `{ error: 'external_outcome_unknown' }`。region 内 **无** `persistTrace…\.catch` 旁路。`releaseSharedAdmissionBestEffort` 的 `.catch` 吞掉 **仅** 槽/钱投影释放，**不得**冒充 persistTrace fail-open。

## 硬钉（勾选）

- [x] **矩阵保持 gap**（行 `UC-E2E-028` + P1-10）；未升 **covered**；未假 **partial**-closed
- [x] EXIT=0 = GAP mark-red（FAIL-OPEN / RECON / TRUTH-BLOCK-E2E / INJECT），**非** A1/A2/A3 产品闭环
- [x] 钉 **同事务耦合 ≠ fail-open**（诚实缺口；fail-closed 成功路径）
- [x] 钉 **R5 green-risk**；本绿 ≠ 全链路 E2E covered
- [x] report-bulkhead / admission best-effort / usage reconciler 标为旁证 ≠ covered
- [x] `releaseEvidence=false` · Not HA（receipt + banner）
- [x] 未因 `mw-model-op` spot 已 pass 而跳过独立复跑 / 放水升档

## 假绿排查（优先：同事务耦合误写成 fail-open covered/partial）

| 风险说法 | 裁定 |
|---------|------|
| `uc028:trace-fail-open:prove` 绿 = **covered** / 「trace 失败不阻塞已通」 | **否** — EXIT=0 = honest gap mark-red；代码仍同事务耦合 |
| prove 绿 = **partial**-closed / 可改矩阵 partial | **否** — harness/eval/P1-10/cite 明示保持 **gap**；升 partial 须至少 fail-open 旁路隔离 + 注入可证（再议）；**本审裁定：不可因 mark-red EXIT=0 改 partial** |
| 「同事务耦合」已诚实钉 → 可读成 fail-open 已接线 / partial | **否** — S2 **反证** fail-open 未接线；耦合 = **缺口**，不是闭环证据 |
| G-GAP EXIT=0 = A1 fail-open / TC-E2E-028-* 已闭环 | **否** — GAP 表示缺席；EXIT=0 仅=诚实钉 |
| `releaseSharedAdmissionBestEffort` `.catch` = persistTrace fail-open | **否** — admission/fee 投影旁路 ≠ `ai_invocation_trace` 旁路隔离 |
| report-bulkhead / Langfuse noop / reqid 绿 = 028 covered | **否** — 旁证 ≠ 本 UC |
| usage-calibration-reconciler 绿 = A2 missing-trace recon | **否** — token 校准 ≠ rewrite/补写队列 |
| isolated/pgvector 绿 = sole-stack / HA / releaseEvidence | **否** — R5 mark-red；receipt `releaseEvidence=false` |
| `mw-model-op` spot pass → 可放水写 covered/partial | **否** — spot 亦钉 gap；本审独立复跑一致，**仍 gap** |
| cite 对部分 UC 接受 partial 分支 → 028 可偷升 | **否** — cite 硬钉 `matrix: UC-E2E-028 remains gap`；本次未发生偷升 |

**gap 是否诚实**：**是**。S1–S5 可执行证明「persistTrace 存在但同事务耦合、无 rewrite、无 e2e、spec≠code」+ G-GAP×4 明示 FAIL-OPEN/RECON/TRUTH-BLOCK/INJECT；矩阵行与 P1-10 均为 **gap**；文档反复钉 ≠ covered / 禁假 partial-closed；**未**把旁证或同事务耦合写成业务 fail-open 闭环。

## 阻塞栏（升 partial / covered / 全链路前必填）

| 阻塞项 | 现状 | 关闭条件 |
|--------|------|----------|
| A1 · persistTrace fail-open 旁路（分事务 / best-effort；失败不回滚业务、不改写 `external_outcome_unknown`） | `GAP-UC028-FAIL-OPEN`；S2 证明同事务耦合 + catch→unknown | 实现旁路隔离 + prove 拒「gap 叙事」后，换 fail-open isolation prove；才可讨论 **partial** |
| A1 · 故障注入：`ai_invocation_trace` 写失败下 interview `completed` + 额度 `confirmed` | `GAP-UC028-INJECT`；无 e2e/夹具 | 注入 harness（权限/约束/mock）可执行并复跑绿后复评 |
| A2 · missing-trace rewrite / 事件账本对账补写 | `GAP-UC028-RECON`；无队列；usage-calibration ≠ UC-028 | 专用 rewrite/recon 路径 + prove 后复评 |
| A3 · 对照 E2E：业务真相写失败必须阻塞 vs trace 失败不得阻塞 | `GAP-UC028-TRUTH-BLOCK-E2E`；仅 inventory | 专用对照 e2e 断言后复评 |
| e2e HTTP/worker 全套 TC-E2E-028-* | S4 证明 e2e 无 UC-028 | isolated HTTP/worker 注入→终态+额度+无二次扣费后复评 |
| 产品面若已浮出旁路/队列/e2e | 当前未浮出（prove 拒浮出仍 EXIT=0） | **不得**继续用本 gap EXIT=0 叙事；须换 isolation prove |
| fixture=pgvector → R5 | isolated 仍 pgvector | 按 M5/sole-stack 迁或持续 mark-red；**不因本绿宣称 HA** |
| 旁证冒充本 UC | report-bulkhead / admission / reqid / usage reconciler | 保持旁证降级；**禁止**借旁证绿宣称 028 covered/partial |
| 假 partial / covered | 矩阵/harness/eval 均 gap | **禁止**因本绿改矩阵；cite 须持续 `remains gap` |

**本切片不因上述阻塞而 block「诚实 gap 登记」pass**；上述 **阻塞升 partial / covered / 宣称 A1 fail-open 闭环 / 把同事务耦合误写成已 fail-open / 把 mark-red 绿写成 partial-closed / 宣称全链路 E2E covered / 宣称 HA·releaseEvidence**。

## 结论与建议

- **pass**；**矩阵裁定：维持 gap**（honest mark-red）；**禁止 covered**；**禁止**因本绿改 **partial**
- 静态诚实面成立：persistTrace 存在但与 settle **同事务耦合（≠fail-open）**、无 rewrite/recon、无 e2e 注入、spec best-effort ≠ code；G-GAP 四钉齐（FAIL-OPEN/RECON/TRUTH-BLOCK-E2E/INJECT）
- 假绿排查通过（含「同事务耦合误读为 fail-open covered/partial」对抗）；cite 钉 `UC-E2E-028 remains gap`；receipt `releaseEvidence=false`
- 与 `reviews/2026-09-10-uc-e2e-028-mw-model-op-spot.md` 一致方向，但本审为 **独立复跑**，不因 spot 放水
- 下一刀（升 partial 门槛）：**优先 persistTrace 旁路拆事务** + 故障注入证明「写失败不阻塞 completed/confirmed」；随后 recon + A3 对照 E2E；在此之前矩阵 **必须 gap**
- 对照：`harness/uc-e2e-028-trace-ledger-fail-open.md` · `eval/uc-e2e-028-trace-ledger-fail-open.eval.md` · `apps/api/test/uc-e2e-028-trace-ledger-fail-open.proof.mjs` · 矩阵 `UC-E2E-028` / P1-10 · spot `reviews/2026-09-10-uc-e2e-028-mw-model-op-spot.md`
