# Meetwise Agent Guide

本仓库采用文档先行、结构先行、契约先行、测试先行的 AI 驱动开发方式。

## 总原则

- 先读 `ai-docs/meta/index.md`，再进入具体产品、架构、规则或测试文档。
- 做实现前，先确认需求来源、业务对象、接口契约、状态机、数据副作用、测试和部署影响。
- 不在没有 PRD、领域模型、接口契约和测试计划的情况下直接生成生产代码；实现前先写清楚测试先行的验收方式。
- 不暗猜接口，不创造未定义接口；前后端必须由共享契约驱动。
- 不把 AI 输出直接当事实；所有模型输出必须经过结构化校验和业务校验。
- 不把简历、聊天记录、模型密钥、支付信息写入日志。
- 不提交真实 `.env`、密钥、简历原文、面试录音或用户敏感数据。

## Codex 项目内执行授权

用户预先授权 Codex 在本仓库范围内完成已明确要求的本地工作，无需为以下常规动作重复征求确认：读取项目文件、修改本仓库内的文件、运行非破坏性的构建、检查和测试命令，以及查看本地 Git 状态与差异。

- 这项授权不放宽本文件的文档先行、契约先行和测试先行门禁；缺少必要来源或验收条件时，先补证据或说明阻塞，不能臆造生产实现。
- 仍须在执行外部写入或不可逆操作前征求确认，包括部署或发布、发送消息/邮件、创建或变更云端资源、购买或付费、修改真实数据、删除重要文件或数据、重写 Git 历史，以及任何实质性扩大任务范围的动作。
- 此处是项目工作流授权，不能覆盖 Codex 客户端、操作系统沙箱或连接器对高风险操作施加的强制审批。

## 工作区边界

- `ai-docs/product`：长期产品目标、术语、领域模型和业务流程。
- `ai-docs/requirements`：具体 epic 和 iteration PRD。
- `ai-docs/architecture`：长期技术架构、模块划分、LangGraph、部署和观测。
- `ai-docs/rules`：长期稳定工程约束。
- `ai-docs/skills`：AI 做某类任务时的工作方法。变更后测试入口：`ai-docs/skills/testing/SKILL.md`。
- `ai-docs/testing`：测试策略（HTTP `e2e:isolated` 主层，Playwright 次层）、TC 规范、golden tasks、E2E 证据和评测。
- `.tmp`：单次执行工单、harness、临时证据、门禁模板和运行记录（不提交）。

## 生成前门禁

涉及代码、数据库、接口、LangGraph、支付、简历隐私、模型调用或测试的任务，至少要先产出：

- 任务范围
- 来源证据
- 明确不做
- 领域对象
- 状态机影响
- 接口契约影响
- 数据库影响
- 测试计划
- 验证命令或阻塞原因

复杂任务按上述字段逐条产出门禁（本地门禁模板见 `.tmp/references/templates/task-harness.md`，不提交）。
