// Deliberately disabled pending MODEL-OP-01. A future smoke must use a typed
// operation, synthetic fixture and durable redacted receipt.
console.error('tts_download_live_smoke_disabled_pending_model_op_01');
process.exit(2);
