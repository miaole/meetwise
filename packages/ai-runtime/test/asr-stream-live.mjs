// Deliberately disabled pending MODEL-OP-01; do not read local audio or send
// media through an unbound streaming provider adapter.
console.error('asr_stream_live_smoke_disabled_pending_model_op_01');
process.exit(2);
