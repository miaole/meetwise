// Deliberately disabled: do not load .env, local audio or a checkpointer for
// a provider smoke before controlled media operations exist.
console.error('voice_live_smoke_disabled_pending_model_op_01');
process.exit(2);
