// Deliberately disabled: raw audio must not be sent to a provider until
// MODEL-OP-01 supplies typed media binding and deletion/attempt contracts.
console.error('asr_live_smoke_disabled_pending_model_op_01');
process.exit(2);
