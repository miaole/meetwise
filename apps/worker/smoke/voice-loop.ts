// Deliberately disabled pending MODEL-OP-01; do not read .env or create live
// media traffic from a developer machine.
console.error('voice_loop_live_smoke_disabled_pending_model_op_01');
process.exit(2);
